# RGS-OPEN-QA-001-ACTIONS-v0.3-feedback-to-agents.md

# 角色：审核 RGS-OPEN-QA-001 v0.2 + RGS-OPEN-QA-001-ACTIONS-v0.3 后的问题反馈单，要求后续 agent session 逐项核实/修改并回填结果
# 生成：主对话（Sonnet 5）2026-08-25，基于本 session 对两份文档的逐条 grep 核实（非仅文本审阅）
# 使用方式：接手 agent 逐条核实 → 修改 → 在对应条目下方追加「已处理」段落说明 commit/依据，不要删除原问题描述（per phase-0-5-feedback-to-agents.md 同款约定）

---

## 0. 审核范围与结论

本轮审核对象：`RGS-OPEN-QA-001_设计制造编程疑问集_v0.1.md`（内容已是 v0.2）+ `RGS-OPEN-QA-001-ACTIONS-v0.3.md`（另一 AI session 按主对话上一轮 handoff 提示词产出，当前 **未提交，git status 显示 untracked**）。

**结论**：ACTIONS-v0.3 是高质量产出——它独立发现了 3 处主对话此前未捕捉的问题（WF-1-55.32~37 WBS 占用冲突、"11 份 B-CODE"的构成错误、NATS 命名沿用而非新造），已逐条用 grep 复核为真。但它自身存在 **5 处内部计数/编号不一致**，且它记录的 9 处修正中有 **4 处只登记在 ACTIONS §5、没有回写进父文档 RGS-OPEN-QA-001 v0.2 答复栏原文** —— 这是本反馈单要处理的核心协调风险。

---

## 1. 【核心】父文档答复栏与 ACTIONS §5 修正表存在"半同步"，下游 agent 若只读父文档会执行过时内容

- **现象**：ACTIONS-v0.3 §5「已知修正」共 9 项。其中：
  - **4 项已回写进 RGS-OPEN-QA-001 v0.2 答复栏正文**（本对话上一轮亲自编辑）：Q-D-01 DTL-037→043、Q-D-02 DTL-038→044、Q-D-03 CROSS-002→001、Q-D-04 RBAC-001→CROSS-007。
  - **4 项只存在于 ACTIONS §5，父文档答复栏原文未同步**：
    - Q-G-02：父文档原文仍写"REV-011 §3 提议 8 个 L4 任务（55.32~41）"；ACTIONS §5#5 已核实实际是 10 个，且 55.32~37 与既有瀑布式 WBS v0.3 任务冲突（**已用 grep 复核，见 §3 本反馈单**）。
    - Q-G-04：父文档原文仍写"11 份 B-CODE log"；ACTIONS §5#6 已核实实际是 7 G-CODE + 4 B-CODE = 11 份。
    - Q-D-09：父文档原文仍写"域.对象.动作"命名提案；ACTIONS §5#7 已核实应沿用既有 `rgs.events.<domain>.<aggregate>.<action>.<version>`（CROSS-003 v0.1 §2.2 已有）。
    - Q-G-03：父文档原文未提及"双轨制"；ACTIONS §5#8 指出 TS-001 v0.6 §6.2 已经是双轨制框架，本动作只是补一段 worktree 共享池参数，不是重新定义。
  - **1 项确认无需修正**（Q-M-10，ACTIONS §5#9），仅做合并，不算不同步。
- **风险**：任何 agent 若只打开 RGS-OPEN-QA-001 v0.2（父文档）执行下游动作，会依据 Q-G-02/Q-G-04/Q-D-09/Q-G-03 的**过时原文**行动，而不知道 ACTIONS-v0.3 §5 已经有修正。这与 phase-0-5-feedback-to-agents.md Issue 1 描述的"并发 session 信息不同步"是同一类风险。
- **要求**：
  1. ~~在 RGS-OPEN-QA-001 v0.2 的 Q-G-02 / Q-G-04 / Q-D-09 / Q-G-03 四条答复栏末尾，各追加一句 `⚠️ 见 RGS-OPEN-QA-001-ACTIONS-v0.3.md §5 修正 #5/#6/#7/#8`（只追加，不改历史原文，per文档footer既有约束）。~~ **✅ 已处理（本反馈单作者 @ 2026-08-25 同批次直接执行，未留给接手 agent）**：4 处均已追加，措辞对应各自修正条目（Q-G-02→#5、Q-G-03→#8、Q-G-04→#6、Q-D-09→#7），均为句末追加，未改动历史原文一字。
  2. 执行任何下游动作前，先读 ACTIONS-v0.3 §5 全表，不要只读父文档。

---

## 2. ACTIONS-v0.3.md 自身 5 处计数/编号内部矛盾（已逐条核实）

| # | 位置 | 矛盾 | 已核实的正确值 |
|---|---|---|---|
| 1 | 表头 L9「约 22 个独立动作」+ §3 标题 L47「22 个独立动作」 vs §1 第 1 点 L30「23 个独立动作」 | 同一文档两种数字 | **23**（A-01~A-10 共 10 + B-01~B-10 共 10 + C-01~C-03 共 3 = 23，实数表格逐行数过）|
| 2 | §3 「B 制造/编程（10 条 → **9** 个动作）」标题（L75） | 标题说 9，下方表格实际列出 B-01~B-10 共 **10** 行 | 标题应改为「10 条 → 10 个动作」或说明具体是哪两条合并成一条（未见合并痕迹）|
| 3 | 修订历史 L20「12 重量级」+ §7 L172「12 个建议 L4」 vs §1 第 4 点 L33「13 重量级动作」+ §4 标题 L100「13 个」 | 同一文档两种数字 | **13**（§4 表格 WF-1-55.38~50 逐行数过，共 13 行）|
| 4 | A-04（L67）编号「① 资源粒度…② 角色枚举…**⑤** OPA/Casbin…」 | 编号从②跳到⑤，缺③④，语义不完整（不知道③④原本要说什么，还是纯笔误） | 需要原作者/接手 agent 补全或改为连续编号 ①②③ |
| 5 | A-03（L66）「① 沿用既有 4 位段（1001-1999 player 等）② 段内子类细分（00-19 校验/20-39 状态冲突…）」 | 两位子类"00-19"如何嵌入四位段（1001-1999）未定义拼接规则——是 `1000-1019` 覆盖到域块起点之前，还是子类是独立于千位段的另一套编码？两种编码方案叠加但未说明组合方式 | 填充 CROSS-001 v0.2 时必须先写死一个具体格式（例如 `域千位段 + 子类两位 = 1000+00~19` 还是 `域千位段内前 20 个号段`），否则会在填充时产生新的编号歧义，重演本文档最初 DTL-037/038 撞号的教训 |

- **要求**：接手 agent 在填充 A-03/A-04 对应文档（CROSS-001/CROSS-007 v0.2）之前，先修正本文档（ACTIONS-v0.3）自身的计数口径和 A-03/A-04 编号方案，避免带着矛盾去指导实际填充工作。

---

## 3. 本反馈单对 ACTIONS-v0.3 关键声明的独立复核记录（供信任判断，非"发现新问题"）

以下是本轮审核用 grep 逐条验证 ACTIONS-v0.3 §5 的核心声明，**均确认属实**：

- **WF-1-55.32~37 占用**：`RGS-WBS-001_瀑布式工作分解结构_v0.3.md` L143-149 确认 55.32=HI-3 fail-closed IT / 55.33=HI-D DC-1 终态 test / 55.34=ME-1 apply_atomic 弃用 / 55.35=ME-2/3 admin migration 注释 / 55.36=ME-4+LO-1/2/3 / 55.37=LO-4 补偿崩溃，与 ACTIONS 描述逐字一致。
- **REV-011「8 个」实际是 10 个**：`RGS-REV-011_5域DTL_6项缺口FollowUp_v0.1.md` §3 表格实测 10 行（WF-1-55.32~41），但该文档自己的标题写"提案汇总: 8 个新 L4 任务"——**这是 REV-011 原文档自身的既有错误**，ACTIONS-v0.3 只是如实指出，不是 ACTIONS 新引入的错误。
- **TS-001 §6.2 双轨制**：v0.6 修订历史 + §6.2.3「双轨对比与切换条件」+ L856「双轨并报原则」均确认存在，且明确「不允许混算」指的是同一域内不能把人·天和 token 平均成一个数，不是禁止两条轨并行汇报——ACTIONS 引用准确。
- **CROSS-003 `rgs.events.*` 命名**：v0.1 §2.2 输出 L37 确认已写 `rgs.events.<domain>.<aggregate>.<action>.<version>` 示例——但要注意，这仍在 §2.2「规划输出」段，§4 文档结构 §1 事件主题命名空间规范目前仍是空骨架（待填充），**不是"已经生效执行"的规范，是"计划要填成这样"的提案**。ACTIONS-v0.3 表述"已定义"方向正确但略有歧义，建议下游 agent 填充时注意这是首次落地，不是复用已有正式条款。
- **CROSS-001 §2.2 千位段方案**：同上，位置也是 §2.2 输出段（非§4正文），性质相同。
- **§8 关联文档"L4 进度表 v0.6"链接指向 `_v0.4.md`**：已核实**不是笔误**——该文件内部「版本」字段确实是 0.6，但文件按其自身修订历史注明的规范「不重命名文件名，避免 git mv 触发无关 diff」，文件名保留 `_v0.4.md`。ACTIONS 引用正确，无需修改。

以下补充 **代码层**核实（此前只审了文档，未查实际 crate 是否存在 ACTIONS 提及的编辑目标——这是本轮审核的补充项）：

- **A-02 提议新 migration `0004_*.sql`**：`crates/player-service/migrations/` 现有 `0001_init.sql`/`0002_outbox.sql`/`0003_outbox_check_idempotent.sql`，当前最大是 `0003`，**`0004` 确实是下一个空闲编号，无冲突**——这正是本反馈单在 §2 #5 提醒 A-03 要预先写死的那类"编号先核实再落笔"检查，A-02 这次做对了。
- **B-04 提议编辑 `shared-platform/src/producer.rs` / `consumer.rs`**：两个文件均**已存在**于 `crates/shared-platform/src/`，编辑目标真实有效。
- **B-03 提议以 `economy-service/tests/integration_outbox.rs` 为 4 域集成测试模板**：该文件**已存在**于 `crates/economy-service/tests/`，可作为模板的前提成立。

---

## 4. ACTIONS-v0.3.md 当前是另一 session 的未提交产出，本 session 不擅自 commit

- **现象**：`git status` 显示 `RGS-OPEN-QA-001-ACTIONS-v0.3.md` 为 untracked，本对话未创建此文件（是按上一轮 handoff 提示词交给另一 AI session 执行后的产出）。
- **依据**：per `RGS-WT-001` §6.6/§11.7 精神（不擅自处理他人未提交产出，phase-0-5-feedback-to-agents.md Issue 1 也是同一先例）。
- **要求**：本反馈单**不会自动 commit** ACTIONS-v0.3.md 或本文件。请 Ulysses 确认后手工提交，或明确授权后续 agent 提交。

---

## 5. 待 Ulysses 拍板的开放问题（不属于"错误"，是需要人决策的分叉）

1. ~~§2 的计数口径以哪个为准~~ **不需要 Ulysses 拍板，可直接判定**：§4（重量级）13 行 + §6（轻量级）10 行，两张表按定义互斥（同一动作不会同时是重量级和轻量级）、无重叠，13+10=23，与逐表数行数得到的 A(10)+B(10)+C(3)=23 完全吻合。这是**穷尽划分**，不存在"隐藏合并意图"的空间——23 是唯一自洽解，"22"和"12"都是笔误（少数漏 1），接手 agent 直接改为 23/13，无需请示。
2. WF-1-55.38~50 是否直接采用，还是要与 REV-011 v0.2（若未来会改版）重新协调编号。
3. A-03「段内两位子类细分」是否真的要做（增加编码复杂度），还是简化为只用四位千位段，不做子类。

---

> 本反馈单由主对话（Sonnet 5）@ 2026-08-25 生成。接手 agent 处理后请在对应条目下追加「已处理」段落，注明 commit hash + 验证证据，不要删除原问题描述。
