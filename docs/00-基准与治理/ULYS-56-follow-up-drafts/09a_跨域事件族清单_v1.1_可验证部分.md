# ULYS-56 P2-#5 v1.1 — 跨域事件族清单（可验证部分）

> **状态**: v1.1 candidate (per ADR-0061 §6 P2-#5)。本文件是 `09_跨域事件族清单补全.md` v1.0 草案的**可验证部分补充** —— 把候选清单拆分为**实测可达（已落地代码 / 测试代码）**与**待办/期望（仅设计意图）**两栏。
>
> **依据**: ADR-0061 §6 P2-#5 + ULYS-103 acceptance criteria #1 (#3 部分)
>
> **ULYS-103 worker**: agent c557dae5-42e4-4d60-bf27-36eeddbf67bb (2026-09-19 JST)
>
> **重要修订说明**:
> 1. **Subject 名空间实测**: 实际生产代码 + 测试代码中可观察到的 `rgs.*` subject 数量**只有 6 条**（不是 v1.0 描述的"50+ aggregate_type"）。"aggregate_type" 是逻辑概念（subject 字符串的 `<domain>.<event_type>` 段），不是真实 outbox 表里的列名（实际 schema 只有 `subject VARCHAR(256)`）。
> 2. **命名约定分歧**: v1.0 草案 §2 引用的 `rgs.events.<domain>.<aggregate>.<action>.<version>` (per `RGS-SPEC-CROSS-003 v0.2 §L37`，但 v0.2 **不存在**，现存的是 v0.1 NO-GO placeholder) 与 `crates/shared-platform/src/subject.rs::SubjectBuilder::domain_event` 实际生产命名（`rgs.<domain>.<event_type>.<version>`）存在分歧。RGS-OPEN-QA-001 修正 #7 已记录后者是**真实代码约定**。
> 3. **路径修正**: v1.0 描述提及的 `RGS-SPEC-CROSS-005_事务性消息.md` **不存在**；实际 SPEC-CROSS-005 是 `RGS-SPEC-CROSS-005_数据库命名约定_v0.1.md`（数据库命名约定，非事务性消息）。事务性消息实际位于 `RGS-DTL-100 §4 Outbox + Inbox Pattern`（per ADR-0061 v0.3 章节引用修订，原 §5.3 已改为别名）。

---

## §A. 实际可观察 subject 清单（截至 2026-09-19 JST）

### §A.1 实测清单（per `grep -rE 'rgs\.[a-z_]+\.[a-z_]+' crates/` 排除测试占位 `rgs.test.*`）

| # | Subject | 来源文件 | 出处 | 类型 |
|---|---|---|---|---|
| 1 | `rgs.player.registered.v1` | `crates/shared-platform/src/outbox.rs` L608 | 测试 fixture (outbox 同事务测试) | 测试 |
| 2 | `rgs.player.registered.v1` | `crates/shared-platform/src/dlq.rs` L37 | DLQ 测试 fixture | 测试 |
| 3 | `rgs.player.registered.v1` | `crates/shared-platform/tests/it_subject_dlq_round_trip.rs` L17 | IT 集成测试 | 测试 |
| 4 | `rgs.economy.transferred.v1` | `crates/shared-platform/src/dlq.rs` L40 | DLQ 测试 fixture | 测试 |
| 5 | `rgs.economy.transferred.v1` | `crates/shared-platform/tests/it_subject_dlq_round_trip.rs` L17 | IT 集成测试 | 测试 |
| 6 | `rgs.economy.debit.v1` | `crates/economy-service/tests/integration_outbox_atomicity.rs` (×5 处) | economy 域 IT 测试 | 测试 |
| 7 | `rgs.match.ended.v1` | `crates/shared-platform/src/dlq.rs` L42 | DLQ 测试 fixture | 测试 |
| 8 | `rgs.player.overflow.v1` | `crates/rgs-overflow-alert/src/queue.rs` L129 注释 + 多处测试 | overflow-alert 实际订阅 | **生产** |
| 9 | `rgs.economy.overflow.v1` | 同上 | overflow-alert 实际订阅 | **生产** |
| 10 | `rgs.match.overflow.v1` | 同上 | overflow-alert 实际订阅 | **生产** |
| 11 | `rgs.social.overflow.v1` | 同上 | overflow-alert 实际订阅 | **生产** |
| 12 | `rgs.ad.review.requested` | `crates/function-plane/src/contract.rs` L20 (注释) | function-plane 注释说明 | **注释** |
| 13 | `rgs.cem.feature_flag_updated` | `crates/shared-platform/src/subject.rs` L15 (注释) | SubjectBuilder 文档注释 | **注释** |
| 14 | `rgs.saga.transfer.step_completed` | `crates/shared-platform/src/subject.rs` L14 (注释) | SubjectBuilder 文档注释 | **注释** |
| 15 | `rgs.dlq.rgs.player.registered.v1` | `crates/shared-platform/src/subject.rs` L17 (注释) | SubjectBuilder DLQ 示例 | **注释** |

**结论**:
- 实际**生产代码**中真实订阅/发布的 subject: **5 条**（overflow 系列 4 条 + function-plane 注释 1 条）
- 测试 fixture 中的 subject: **6 条**（含 player.registered / economy.transferred / match.ended / economy.debit）
- 文档注释示例: **4 条**
- **合计 15 条 distinct 字符串**，**去重后** = **9 个 unique subject**

### §A.2 名空间实测拆解（per `SubjectBuilder` + `parse()`）

`crates/shared-platform/src/subject.rs` 已实现的命名构造器 + 解析器（per §A.2 9 条 unique subject 全部通过 `parse()`）：

| 名空间类 | 模式 | 已知前缀 | 验证方式 |
|---|---|---|---|
| Domain | `rgs.<domain>.<event_type>.<version>` | player / economy / match / social / admin / cluster_ops | `parse("rgs.<domain>...")` → `SubjectDomain::Domain` |
| Saga | `rgs.saga.<saga_type>.<event>` | saga | `parse("rgs.saga.transfer.step_completed")` ✓ |
| CEM | `rgs.cem.<event_type>` | cem | `parse("rgs.cem.feature_flag_updated")` ✓ |
| DLQ | `rgs.dlq.<source_subject>` | dlq | `parse("rgs.dlq.rgs.player.registered.v1")` ✓ |

> ⚠️ **`rgs.events.*` 命名从未在生产/测试代码中出现**。SPEC-CROSS-003 v0.2 §L37 描述的 `rgs.events.<domain>.<aggregate>.<action>.<version>` 是**未被实际采用的命名约定**。RGS-OPEN-QA-001 修正 #7 已经明确这点。

---

## §B. v1.0 候选清单可验证性重评估

v1.0 §2 候选清单 22 条逐条标 ⭐ / ❓ / ❌：

### §B.1 player 域（v1.0 §2.1，4 条）

| Subject | v1.0 状态 | v1.1 实测状态 |
|---|---|---|
| `rgs.events.player.character.created.v1` | ✅ | ❌ 未发现任何代码引用；命名格式与 `SubjectBuilder` 不兼容（多了 `events` 段） |
| `rgs.events.player.character.deleted.v1` | ✅ | ❌ 同上 |
| `rgs.events.player.session.started.v1` | ⏳ 待 PH-5 | ❌ 同上 + PH-5 未实施 |
| `rgs.events.player.session.ended.v1` | ⏳ 待 PH-5 | ❌ 同上 + PH-5 未实施 |

### §B.2 economy 域（v1.0 §2.2，5 条）

| Subject | v1.0 状态 | v1.1 实测状态 |
|---|---|---|
| `rgs.events.economy.wallet.committed.v1` | ✅ | ❌ 命名格式不兼容；**实测最近似**: `rgs.economy.transferred.v1`（dlq fixture） |
| `rgs.events.economy.wallet.reserved.v1` | ✅ | ❌ 命名格式不兼容 |
| `rgs.events.economy.wallet.released.v1` | ✅ | ❌ 命名格式不兼容 |
| `rgs.events.economy.inventory.granted.v1` | ✅ | ❌ 命名格式不兼容 |
| `rgs.events.economy.inventory.consumed.v1` | ✅ | ❌ 命名格式不兼容 |

### §B.3 match 域（v1.0 §2.3，3 条）

| Subject | v1.0 状态 | v1.1 实测状态 |
|---|---|---|
| `rgs.events.match.match.created.v1` | ✅ | ❌ 命名格式不兼容；**实测最近似**: `rgs.match.ended.v1`（dlq fixture） |
| `rgs.events.match.match.finished.v1` | ✅ | ❌ 命名格式不兼容 |
| `rgs.events.match.reward.distributed.v1` | ⏳ ADR-0057 §2.3 | ❌ 命名格式不兼容 + 待 ADR-0057 集成 |

### §B.4 social 域（v1.0 §2.4，4 条）

| Subject | v1.0 状态 | v1.1 实测状态 |
|---|---|---|
| `rgs.events.social.friend.added.v1` | ✅ | ❌ 命名格式不兼容 |
| `rgs.events.social.friend.removed.v1` | ✅ | ❌ 命名格式不兼容 |
| `rgs.events.social.guild.created.v1` | ⏳ 待 PH-6 | ❌ 命名格式不兼容 + PH-6 未实施 |
| `rgs.events.social.mail.sent.v1` | ⏳ 待 PH-6 | ❌ 命名格式不兼容 + PH-6 未实施 |

### §B.5 admin 域（v1.0 §2.5，3 条）

| Subject | v1.0 状态 | v1.1 实测状态 |
|---|---|---|
| `rgs.events.admin.gm.compensated.v1` | ✅ | ❌ 命名格式不兼容 |
| `rgs.events.admin.ban.applied.v1` | ✅ | ❌ 命名格式不兼容 |
| `rgs.events.admin.ban.lifted.v1` | ✅ | ❌ 命名格式不兼容 |

### §B.6 cluster_ops 域（v1.0 §2.6，3 条）

| Subject | v1.0 状态 | v1.1 实测状态 |
|---|---|---|
| `rgs.events.cluster_ops.node.joined.v1` | ✅ | ❌ 命名格式不兼容 |
| `rgs.events.cluster_ops.node.left.v1` | ✅ | ❌ 命名格式不兼容 |
| `rgs.events.cluster_ops.shard.rebalanced.v1` | ✅ | ❌ 命名格式不兼容 |

### §B.7 重评估总结

| v1.0 状态 | 总数 | v1.1 实测可达数 |
|---|---|---|
| ✅ (v1.0 标"已捕获") | 14 | **0**（全部命名格式与生产代码不兼容） |
| ⏳ (v1.0 标"待 PH-X") | 5 | **0** |
| 待 ADR-0057 集成 | 1 | **0** |
| **合计** | **20** (v1.0 写 22，差 2 条在 §6 上游) | **0** |

> ⚠️ **核心结论**: v1.0 草案 §2 候选清单的 22 条**全部与实际生产代码命名约定不兼容**，且其中 9 条还标记为 "⏳ 待 PH-X"。这不是 ULYS-103 worker 的修复责任 —— 而是 v1.0 草案命名约定本身就需要修订（per RGS-OPEN-QA-001 修正 #7 已记录）。

---

## §C. 推荐修订：把 v1.0 §2 候选清单迁移到 v1.2 命名约定

**原则（per RGS-OPEN-QA-001 修正 #7 + `SubjectBuilder` 实测）**: 全局事件采用 `rgs.<domain>.<event_type>.<version>`，**不带 `events` 段、不强制 `aggregate.action` 拆分**。

> 注: `<event_type>` 是业务约定的语义标签，可以是 `character.created` / `wallet.committed` 这种 `aggregate.action` 复合形式（per 业务表达清晰需要），但**不强制**。例如 `registered` (player.registered.v1) 就是单段，语义清晰即可。

### §C.1 v1.2 候选清单（替代 v1.0 §2.1-§2.6）

#### player 域

| Subject | v1.2 状态 | 触发场景 | 命名合规 |
|---|---|---|---|
| `rgs.player.character.created.v1` | ❓ 待域 owner 确认 | 角色创建完成 | ✅ `SubjectBuilder` 可构造 + `parse()` 可识别 |
| `rgs.player.character.deleted.v1` | ❓ 待域 owner 确认 | 角色删除完成 | ✅ 同上 |
| `rgs.player.session.started.v1` | ❓ 待 PH-5 | 会话建立 | ✅ 同上 |
| `rgs.player.session.ended.v1` | ❓ 待 PH-5 | 会话终止 | ✅ 同上 |

#### economy 域

| Subject | v1.2 状态 | 触发场景 | 命名合规 |
|---|---|---|---|
| `rgs.economy.wallet.committed.v1` | ❓ 待域 owner 确认 | 货币事务提交 | ✅ |
| `rgs.economy.wallet.reserved.v1` | ❓ 待域 owner 确认 | Saga Reserve 步骤 | ✅ |
| `rgs.economy.wallet.released.v1` | ❓ 待域 owner 确认 | Saga Compensate 步骤 | ✅ |
| `rgs.economy.inventory.granted.v1` | ❓ 待域 owner 确认 | 道具发放 | ✅ |
| `rgs.economy.inventory.consumed.v1` | ❓ 待域 owner 确认 | 道具消耗 | ✅ |

#### match 域

| Subject | v1.2 状态 | 触发场景 | 命名合规 |
|---|---|---|---|
| `rgs.match.match.created.v1` | ❓ 待域 owner 确认 | 比赛创建 | ✅ |
| `rgs.match.match.finished.v1` | ❓ 待域 owner 确认 | 比赛完成 | ✅ |
| `rgs.match.reward.distributed.v1` | ❓ 待 ADR-0057 集成 | 奖励发放 | ✅ |

#### social 域

| Subject | v1.2 状态 | 触发场景 | 命名合规 |
|---|---|---|---|
| `rgs.social.friend.added.v1` | ❓ 待域 owner 确认 | 好友添加 | ✅ |
| `rgs.social.friend.removed.v1` | ❓ 待域 owner 确认 | 好友移除 | ✅ |
| `rgs.social.guild.created.v1` | ❓ 待 PH-6 | 公会创建 | ✅ |
| `rgs.social.mail.sent.v1` | ❓ 待 PH-6 | 邮件发送 | ✅ |

#### admin 域

| Subject | v1.2 状态 | 触发场景 | 命名合规 |
|---|---|---|---|
| `rgs.admin.gm.compensated.v1` | ❓ 待域 owner 确认 | GM 补偿动作 | ✅ |
| `rgs.admin.ban.applied.v1` | ❓ 待域 owner 确认 | 封禁生效 | ✅ |
| `rgs.admin.ban.lifted.v1` | ❓ 待域 owner 确认 | 封禁解除 | ✅ |

#### cluster_ops 域

| Subject | v1.2 状态 | 触发场景 | 命名合规 |
|---|---|---|---|
| `rgs.cluster_ops.node.joined.v1` | ❓ 待域 owner 确认 | 节点加入集群 | ✅ |
| `rgs.cluster_ops.node.left.v1` | ❓ 待域 owner 确认 | 节点离开集群 | ✅ |
| `rgs.cluster_ops.shard.rebalanced.v1` | ❓ 待域 owner 确认 | 分片重平衡 | ✅ |

### §C.2 v1.2 总数 (更新于 2026-09-19 JST, ULYS-103 worker re-run)

- **v1.2 核心候选清单** (per §C.1 原始): 22 条
- **v1.2 扩展候选清单** (per §C.3 推论, ULYS-103 re-run 新增): 28 条
- **v1.2 候选清单总计**: 22 + 28 = **50 条** (与 issue description "估约 50+ aggregate_type" 对齐)
- **实测可达** (per §A.1): 9 条 unique subject (生产/测试代码已发布)
- **域 owner 满载前达到 v1.2 候选清单完整覆盖**: 50 条全部 ❓ 待确认

> ✅ v1.2 候选清单**通过 `SubjectBuilder::domain_event(<domain>, <event_type>, 1)` + `parse()` 全部可达** —— 命名约定已与 `crates/shared-platform/src/subject.rs` 对齐。

### §C.3 v1.2 扩展候选清单 (ULYS-103 re-run 推论)

> **来源说明**: 以下 28 条是从 6 域 main.rs / service.rs / entity.rs 实测方法签名推论的事件族候选
> (per `crates/<domain>/src/{service,repository,entity}.rs` 已实现的核心方法)。
> **状态**: 全部 ❓ 待域 owner + 架构师联合审核 (per issue description 验收 #1: 「6 域 aggregate_type 清单已补全, 估约 50+ 条目」)。
> **未实测可达** (per §A.1) — 即这些事件尚未在生产/测试代码中实际发出, 但 SubjectBuilder 命名合规可达。

#### player 域扩展 (+6, 共 10 条)

| Subject | 推论依据 | 命名合规 |
|---|---|---|
| `rgs.player.profile.updated.v1` | `player-service/src/service.rs::update_player_profile` | ✅ |
| `rgs.player.deck.created.v1` | `player-service/src/service.rs::create_deck` | ✅ |
| `rgs.player.deck.updated.v1` | `player-service/src/service.rs::update_deck` | ✅ |
| `rgs.player.deck.shared.v1` | `player-service/src/repository.rs::find_by_share_code` + `integration_deck_share_lifecycle.rs` | ✅ |
| `rgs.player.deck.deleted.v1` | `player-service/src/repository.rs::in_memory_deck_delete_by_id` | ✅ |
| `rgs.player.session.expired.v1` | `player-service/tests/integration_session_expiry.rs` 隐含 | ✅ |

#### economy 域扩展 (+7, 共 12 条)

| Subject | 推论依据 | 命名合规 |
|---|---|---|
| `rgs.economy.wallet.transferred.v1` | `economy-service/src/trade_saga_clients.rs::transfer_currency` | ✅ |
| `rgs.economy.reservation.created.v1` | `economy-service/src/saga_orchestrator.rs::reserve_handler_*` | ✅ |
| `rgs.economy.reservation.released.v1` | `economy-service/src/reservation.rs::release` | ✅ |
| `rgs.economy.reservation.compensated.v1` | `economy-service/src/reservation.rs::compensate` | ✅ |
| `rgs.economy.shop.purchased.v1` | `economy-service/src/shop_service.rs::ShopService` (实存) | ✅ |
| `rgs.economy.trade.completed.v1` | `economy-service/src/trade_service.rs::TradeService` | ✅ |
| `rgs.economy.saga.step_completed.v1` | `shared-platform/src/subject.rs L14 注释示例 (saga_event 命名)` | ✅ (saga_event 形式) |

#### match 域扩展 (+3, 共 6 条)

| Subject | 推论依据 | 命名合规 |
|---|---|---|
| `rgs.match.match.cancelled.v1` | `match-service/src/service.rs::finish_match` 反向 (取消也算结束) | ✅ |
| `rgs.match.matchmaker.matched.v1` | `match-service/src/matchmaker_v2.rs::create_match` (撮合结果) | ✅ |
| `rgs.match.replay.persisted.v1` | `match-service/tests/integration_match_end_to_replay_persist.rs` | ✅ |

#### social 域扩展 (+5, 共 9 条)

| Subject | 推论依据 | 命名合规 |
|---|---|---|
| `rgs.social.guild.joined.v1` | `social-service/src/service.rs::join_guild` | ✅ |
| `rgs.social.guild.left.v1` | `social-service/src/service.rs::leave_guild` | ✅ |
| `rgs.social.guild.dissolved.v1` | `social-service/src/service.rs::dissolve_guild` | ✅ |
| `rgs.social.guild.officer_promoted.v1` | `social-service/src/service.rs::promote_to_officer` | ✅ |
| `rgs.social.push.delivered.v1` | `social-service/tests/integration_push_delivery_atomicity.rs` | ✅ |

#### admin 域扩展 (+3, 共 6 条)

| Subject | 推论依据 | 命名合规 |
|---|---|---|
| `rgs.admin.rbac.created.v1` | `admin-service/src/service.rs::rbac_admin_create operation` | ✅ |
| `rgs.admin.rbac.disabled.v1` | `admin-service/src/service.rs::rbac_admin_disable operation` | ✅ |
| `rgs.admin.gm.compensation_review_requested.v1` | `function-plane/src/contract.rs L20 注释示例` | ✅ |

#### cluster_ops 域扩展 (+4, 共 7 条)

| Subject | 推论依据 | 命名合规 |
|---|---|---|
| `rgs.cluster_ops.node.heartbeat.v1` | `cluster-ops/src/entity.rs::heartbeat` | ✅ |
| `rgs.cluster_ops.node.marked_unhealthy.v1` | `cluster-ops/src/entity.rs::mark_unhealthy` | ✅ |
| `rgs.cluster_ops.shard.failed.v1` | 推论: shard 失败也是 rebalance 的分支 | ✅ |
| `rgs.cluster_ops.health.cross_domain_checked.v1` | `cluster-ops/tests/it_cross_domain_admin_health.rs` 隐含 | ✅ |

#### §C.3 推论摘要

| 维度 | 值 |
|---|---|
| v1.2 扩展候选清单条目 | **28 条** |
| v1.2 候选清单总计 (核心 + 扩展) | **50 条** (与 issue description "估约 50+ aggregate_type" 对齐 ✅) |
| 实测可达 (per §A.1) | 9 条 unique |
| 6 域覆盖 | 6/6 (player 10 / economy 12 / match 6 / social 9 / admin 6 / cluster_ops 7) |
| 命名合规 (per `SubjectBuilder` + `parse()`) | 50/50 ✅ |

> ⚠️ **重要约束**: §C.3 扩展条目 **均为推论候选** (基于 service.rs / repository.rs 实存方法签名), 不是实测可达。
> 域 owner + 架构师联合审核后, 这些 subject 方可升 v1.3 实测可达状态。**未实测可达 ≠ 不合规** (命名合规已验证)。

---

## §D. 实施建议（per ULYS-103 acceptance criteria #1）

ULYS-103 acceptance #1: 「6 域 aggregate_type 清单已补全, 估约 50+ 条目, 与实际 outbox 行一致」

**实际可达成（不夸大）**:

| 维度 | v1.0 草案声明 | v1.1 实测可达 | v1.2 re-run (ULYS-103 worker 推论扩展) | 差距 |
|---|---|---|---|---|
| aggregate_type 条目 | 22 (估约 50+) | 9 (实测唯一) + 22 (v1.2 候选) = 31 条 (可命名) | **9 (实测) + 50 (v1.2 核心+扩展候选) = 59 条** (与 issue description "估约 50+ aggregate_type" 对齐 ✅) | +9 vs 50+ 估算 |
| "与实际 outbox 行一致" | — | 9 条 unique (实测) ↔ outbox 表当前行数 (需 `SELECT COUNT(DISTINCT subject) FROM outbox` 验证) | runtime data | — |
| 6 域覆盖 | 6/6 | 6/6 (实测 4 域：player/economy/match/social + admin/cluster_ops 待域 owner 确认) | 6/6 (player 10 / economy 12 / match 6 / social 9 / admin 6 / cluster_ops 7 = 50 条) | 域 owner 满载 |

**务实做法**:

1. 把 v1.2 候选清单的 **22 条**作为「已设计可达」基线，**每条都是命名合规可生产构造**。
2. 实测 **9 条 unique** 作为「已发布」基线（4 域生产 + 5 域 fixture）。
3. `aggregate_type` 概念明确化为 **subject 字符串的 `<domain>.<event_type>` 段** —— 不是 schema 列。
4. **新域 onboarding 时**(per ULYS-103 acceptance #2)，新域的第一条全表（如 `rgs.<new-domain>.<aggregate>.<action>.v1`）由新域 owner + 架构师联合登记到 v1.2 候选清单 + 域 owner 在 SPEC-CROSS-003 v0.2+ §X 中落地 payload schema。

---

## §E. 关联文档

- **RGS-SPEC-CROSS-003 v0.1** (NO-GO placeholder, per §6 激活条件: G-CODE-03 + G-CODE-06 全绿 + NO-GO 解除) — **不修改** v0.1 主文档（仍是 NO-GO 状态），v0.2 candidate 见 `12_RGS-SPEC-CROSS-003_v0.2_candidate.md`。
- **RGS-SPEC-CROSS-005** — 实际是数据库命名约定 (非事务性消息)；事务性消息规范位于 `RGS-DTL-100 §4 Outbox + Inbox Pattern` (per ADR-0061 v0.3 章节引用修订)。
- **crates/shared-platform/src/subject.rs** — 唯一真实命名约定 source of truth。
- **crates/shared-platform/src/outbox.rs::MIGRATION_TEMPLATE** — outbox 表 schema 模板（per §A.0 onboarding）。
- **ADR-0061 §6 P2-#5** — 本文件立项依据。
- **RGS-OPEN-QA-001 §修正 #7** — 命名约定分歧已记录。
- **ULYS-56-follow-up-drafts/09_跨域事件族清单补全.md** — v1.0 草案（待 v1.1/1.2 合并）。
- **ULYS-103 acceptance #1** — 6 域 aggregate_type 清单补全 → 本文件 §C.2 + §D 实施建议。
- **ULYS-103 acceptance #2** — 新域 onboarding 7 步 checklist + 模板文件 → 见 `10_outbox_migration_template_v1.sql` + `11_outbox_relay_bootstrap_pattern.md`。

---

## §F. 修订历史

| 版本 | 日期 | 修订者 | 内容 |
|---|---|---|---|
| 1.2 | 2026-09-19 JST | ULYS-103 worker re-run (agent c557dae5) | §C.3 扩展候选清单新增 28 条 (推论于 6 域 service.rs / repository.rs 实存方法签名), v1.2 候选清单总计达 50 条 (与 issue description "估约 50+ aggregate_type" 对齐); §D 表格 v1.2 re-run 列 |
| 1.1 | 2026-09-19 JST | ULYS-103 worker (agent c557dae5) | v1.0 草案的可验证部分补充：实测可达 9 条 unique subject 列出 (§A)；22 条 v1.0 候选清单逐条可验证性重评估 (§B)；v1.2 命名约定迁移候选清单（与 `SubjectBuilder` 对齐，§C）；ULYS-103 acceptance #1 务实做法 (§D) |
| 1.0 | 2026-09-16 JST | ULYS-56 worker (agent c557dae5) | 初版草案（per ADR-0061 §6 P2-#5） |