# ULYS-56 P1-#4 补注候选: RGS-DTL-100 §5.3 事务性消息补注

> **状态**: 待具名人类审批 (per ADR-0061 状态)。本文件是**候选草案**, ADR-0061 通过后由架构师正式合并至 `docs/01-核心架构与设计模式/RGS-DTL-100_Saga业务模式设计_v0.1.md`。
>
> **依据**: RGS-ADR-0061 §2 决定 4 第 6 项 + §6 P1-#4
>
> **重要差异说明**: ADR-0061 §1 第 12 行 + §2 决定 4 第 6 项均引用 "RGS-DTL-100 §5.3 事务性消息"。**经核实, RGS-DTL-100 实际章节为 §4 Outbox + Inbox Pattern**（L403）, **无 §5.3**。§5 是 Reservation 流程（L505）, 与事务性消息无直接关联。本候选补注将修订位置指向 §4 (实际位置), 并在 RGS-ADR-0061 未来修订时反馈此差异。
>
> **ULYS-56 工作范围声明**: 本文件仅提供候选补注文本, 不直接修改 RGS-DTL-100。

---

## 1. §4 Outbox + Inbox Pattern 节补注

**插入位置**: `docs/01-核心架构与设计模式/RGS-DTL-100_Saga业务模式设计_v0.1.md` §4.1 Outbox 模式（生产端）段末（L403-450 区间, mermaid sequenceDiagram 后）。

**新增段** (在 §4.1 mermaid 流程图后追加):

```markdown
> **工程方案（per RGS-ADR-0061, 待具名人类审批, 2026-09-15 ULYS-56 起草）**：
>
> §4.1 Outbox 模式（生产端）mermaid 流程图所述「业务事务内 INSERT outbox + 后台 Worker 轮询发布」由 **自研 4 状态机 outbox_worker** 实现, **不引入 Debezium CDC**（per RGS-ADR-0061 §1 全段）。本节是该实现的工程方案详细补充, 与 RGS-ADR-0061 §3.1 备选否决论证（拒绝 Debezium CDC）一致。
>
> 1. **事务边界强制**: 业务 DML 与 outbox INSERT **必须**在同一 SQL 事务内完成。`crates/shared-platform/src/outbox.rs::OutboxRepository::append`（L162-195）接受泛型 `PgExecutor`, 调用方把业务 DML 和 outbox INSERT 包在 `pool.begin().await?` 返回的 `Transaction` 内提交。**非事务内的 DB 变更不会被传播**（per RGS-ADR-0061 §2 决定 2 约束正式化）。
>
> 2. **4 状态机驱动轮询**:
>    - `Pending` (初始, 待发布) → `InFlight` (Worker 持锁, `lease_until = NOW() + 30s`)
>    - `InFlight` → `Sent` (NATS JetStream 发布成功 ACK, `published_at = NOW()`)
>    - `InFlight` → `Pending` (lease 过期被 reclaim, 或发布失败无 ACK)
>    - 任意状态 → `Failed` (重试超阈值, `last_error` 字段记录, 待 DLQ 处置)
>    - 详见 `crates/shared-platform/src/outbox.rs` L52-75 (OutboxStatus 枚举 + Lease 默认 30s)
>
> 3. **多 relay 并发安全**: 6 域各自 `main.rs` 启动 outbox relay 后台轮询, **多副本并发** 通过 PostgreSQL 原生 `SELECT ... FOR UPDATE SKIP LOCKED` + 30s lease 机制实现（`outbox.rs` L24-26 注释 + L271 reclaim 实现）。无 Debezium Connect 单实例 OR cluster 限制。
>
> 4. **去重与幂等**: 消费者侧 Inbox（per §4.2）按 `event_id` 去重, 与 §4.2 Inbox 模式（消费端）配合实现 at-least-once + 幂等消费。
>
> 5. **CDC 路径偏离参考设计**: 参考设计 §16-18 默认 `PostgreSQL WAL → Debezium → Kafka`, RGS 实际采用本节所述 `事务内强制 Outbox INSERT → 自研 outbox_worker 轮询 → NATS JetStream`（per RGS-ADR-0060 NATS 偏离 + ADR-0061 CDC 偏离, 待具名人类审批）。**Debezium 不引入**（per RGS-ADR-0061 §1.4 评估: 主项目 Apache-2.0 合规, 但 RGS 拒绝理由是 OLU + 设计替代性, 非 BR-111 合规）。
```

---

## 2. ADR-0061 章节引用修订建议 (反馈至 ADR-0061 起草者)

**说明**: ADR-0061 多次引用 "RGS-DTL-100 §5.3 事务性消息", 但实际文档章节为 §4 Outbox + Inbox Pattern。**此差异不影响 ADR-0061 实质论证**, 但下游级联合并时建议修订 ADR-0061:

| ADR-0061 引用位置 | 当前引用 | 建议修订为 |
|---|---|---|
| §1 第 12 行 "关联下游文档" | RGS-DTL-100 §5.3 事务性消息 | RGS-DTL-100 §4 Outbox + Inbox Pattern |
| §2 决定 4 第 6 项 | RGS-DTL-100 §5.3 补注 | RGS-DTL-100 §4 补注 |
| §5 关联 "依赖本决策" | RGS-DTL-100 §5.3 事务性消息 | RGS-DTL-100 §4 Outbox + Inbox Pattern |
| §6 P1-#4 | RGS-DTL-100 §5.3 补注 | RGS-DTL-100 §4 补注 |

**此修订属 ADR-0061 v0.2+ 候选修订内容, 候选操作者 (架构师) 在 ADR 正式合并前可一并更正**。

---

## 3. 候选操作者签字栏

| 角色 | 签字 | 日期 |
|---|---|---|
| 起草 (ULYS-56 worker) | worker (agent c557dae5) | 2026-09-16 JST |
| 候选操作者 (合并至 DTL-100) | 架构师（Ulysses 一人公司兼任 per DEC-008） | 待 ADR-0061 具名审批通过后 |

---

## 4. 关联文档

- **RGS-ADR-0061 §1 全段** (偏离事实)
- **RGS-ADR-0061 §2 决定 2** (事务内强制 outbox 写入约束)
- **RGS-ADR-0061 §3.1** (备选否决: Debezium CDC)
- **RGS-ADR-0061 §6 P1-#4**: 后续工作项
- **`crates/shared-platform/src/outbox.rs`**: L10-11 (文件头注释) + L24-26 (FOR UPDATE SKIP LOCKED) + L52-75 (OutboxStatus 枚举 + Lease 默认 30s) + L162-195 (`append` 接受 PgExecutor) + L271 (reclaim)
- **`crates/shared-platform/src/outbox_relay.rs`**: OutboxRelay + RelayConfig 调度
- **RGS-DTL-100 §4 Outbox + Inbox Pattern** (现需补注, 实际位置, 与 ADR-0061 §5.3 引用有差异)