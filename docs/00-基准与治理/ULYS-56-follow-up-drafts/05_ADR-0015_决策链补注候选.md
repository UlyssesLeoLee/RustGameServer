# ULYS-56 P2-#1 补注候选: RGS-ADR-0015 决策链补注

> **状态**: 待具名人类审批 (per ADR-0061 状态)。本文件是**候选草案**, ADR-0061 通过后由架构师正式合并至 `docs/08-架构决策记录/RGS-ADR-0015_工作流（Saga）适用边界与单一调解者原则.md`。
>
> **依据**: RGS-ADR-0061 §2 决定 4 第 7 项 + §6 P2-#1
>
> **ULYS-56 工作范围声明**: 本文件仅提供候选补注文本, 不直接修改 RGS-ADR-0015。

---

## 1. ADR-0015 §2 决定段补注

**插入位置**: `docs/08-架构决策记录/RGS-ADR-0015_工作流（Saga）适用边界与单一调解者原则.md` §2 决定段（L24-29）末, 在两条规则后追加备注段。

**新增段** (L29 "2. **单一调解者...**" 句后追加):

```markdown
> **隐含 Outbox 路径备注（per RGS-ADR-0061, 待具名人类审批, 2026-09-15 ULYS-56 起草）**：
>
> 本 ADR §1 背景提及「ADR-0007 把高频经济操作收进单一事务边界后, 仍存在确实跨限界上下文的长流程」, 该背景下"事务内强制 outbox 写入"是隐含的工程路径——但本 ADR 未显式归档, 直至 RGS-ADR-0061（CDC / Outbox 偏离参考设计, 待具名人类审批）正式将这条隐含路径沉淀为 ADR 记录。
>
> 关联要点:
> 1. **6 域 outbox 实装**（per RGS-ADR-0061 §1.3）: admin / cluster_ops / economy / match / player / social 各自持有 outbox 表 + 启动 outbox relay
> 2. **事务边界强制**: Saga 步骤 Reserve / Commit / Compensate 的每次状态变更都附带 outbox 事件（per RGS-DTL-100 §1.3 购买 Saga + §3.2 角色创建 Saga + §3.3 比赛奖励 Saga 时序图）, 与 ADR-0007 单事务边界互补——同上下文内 Saga 步骤走单事务, 跨上下文通过 outbox + relay 异步传播
> 3. **唯一可查询流程状态**: 本 ADR §2 规则 2 要求"每个 Saga 必须有唯一一个明确的调解者组件持有流程状态", 与 outbox 4 状态机（Pending/InFlight/Sent/Failed）的 `saga_instance` 表状态机正交——saga_instance 是调解者状态, outbox 是事件传播通道
> 4. **Debezium 不引入**: 自研 Outbox 4 状态机取代 Debezium CDC（per RGS-ADR-0061 §1 + §3.1 否决）, 失去"捕获所有 DB 变更"能力的 trade-off 由"事务内强制 outbox 写入"约束承接（per RGS-ADR-0061 §2 决定 2）
```

---

## 2. ADR-0015 §5 关联段补注

**插入位置**: §5 关联段（L53-57）末追加一行。

**新增行** (在 L57 "RGS-BAS-016（支付对账）" 后追加):

```markdown
- 协同决策: RGS-ADR-0061 CDC / Outbox 偏离参考设计（待具名人类审批）—— 本 ADR 隐含的"事务内强制 outbox 写入"路径由 ADR-0061 正式归档, 6 域 outbox 实装证据清单见 ADR-0061 §1.3
```

---

## 3. ADR-0015 修订历史追加

**插入位置**: §修订历史段（§5 后, 文档末）追加一行。**注**: 当前 ADR-0015 §7 修订历史未在文档中显式列出（仅 §1 标注"制定日 2026-08-17, 制定者 架构师"），新增"修订历史"段如下:

```markdown
## 7. 修订历史

| 版本 | 修订日 | 修订者 | 修订内容 |
|---|---|---|---|
| 0.1 | 2026-08-17 | 架构师 | 初版制定（per §1 头表） |
| 0.2 | 2026-09-xx JST | 架构师（Ulysses 一人公司兼任 per DEC-008）| per RGS-ADR-0061（待具名人类审批）, §2 决定段新增"隐含 Outbox 路径备注", §5 关联段新增 ADR-0061 协同决策引用 |
```

---

## 4. 候选操作者签字栏

| 角色 | 签字 | 日期 |
|---|---|---|
| 起草 (ULYS-56 worker) | worker (agent c557dae5) | 2026-09-16 JST |
| 候选操作者 (合并至 ADR-0015) | 架构师（Ulysses 一人公司兼任 per DEC-008） | 待 ADR-0061 具名审批通过后 |

---

## 5. 关联文档

- **RGS-ADR-0061 §1.3** (6 域 outbox 实装证据清单)
- **RGS-ADR-0061 §2 决定 2** (事务内强制 outbox 写入约束)
- **RGS-ADR-0061 §6 P2-#1**: 后续工作项
- **RGS-ADR-0007** (前置决策: 高频操作收进单事务)
- **RGS-ADR-0059 / ADR-0060** (三面治理漏洞闭合: 缓存 + 事件总线 + CDC)
