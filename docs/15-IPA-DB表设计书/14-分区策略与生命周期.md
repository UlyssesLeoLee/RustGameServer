# 14-分区策略与生命周期（Partitioning & Lifecycle）

> **本文件定位**：RGS 仓库全部 42 张表的**分区策略 + 保留期 + 生命周期管理**总览。依据 RGS-BAS-007 §4 既定分区标准 + 业务实际规模。

| 项目 | 内容 |
|---|---|
| 文档编号 | RGS-DTL-DB-PARTITION |
| 版本 | 0.1 |
| 作成日 | 2026-09-01 JST |
| 適用範囲 | RGS 全部 PG 库（SQLite 库不支持分区） |

---

## 1. 分区策略总览（per RGS-BAS-007 §4）

| 表类别 | 分区策略 | 保留期 | RGS 仓库现状 |
|---|---|---|---|
| `operation_audit` (admin_db) | 按月范围分区（Range on `occurred_at`） | 3 年（36 个分区滚动） | **未实施**（P0-02）|
| `outbox` × 6 域 | 按周/月范围分区（依吞吐量，PH-5 明确）| 短期（已发布清理） | **未实施**（P0-03）|
| 行为日志/分析数据表（PH-6）| 按天/周范围分区 | 400 天 | 暂无 PH-6 表 |
| 幂等去重记录表（`request_id` 已处理、`event_id` 去重）| 按时间范围分区 | 7 天（业务允许最大重试窗口）| `inbox` (economy_db) **未实施** |
| 业务主表（`players` / `accounts` / `inventory` 等）| **不分区**（未证明前不引入复杂性）| 不适用 | — |
| `realm_lifecycle_run` (admin_db) | 按月范围分区（Range on `created_at`）| 3 年 | **已实施** ✅ |

---

## 2. 已实施分区

### 2.1 `realm_lifecycle_run` (admin_db) — 已按月分区

**出处**：`crates/cluster-ops/migrations/0020_lcm_tables.sql:47` + DO 块 line 51-67

```sql
CREATE TABLE IF NOT EXISTS realm_lifecycle_run (
    ...
    created_at TIMESTAMPTZ NOT NULL DEFAULT now(),
    ...
) PARTITION BY RANGE (created_at);

-- 初始分区：当月 + 下月（per DO 块）
DO $$
DECLARE
    current_month_start TIMESTAMPTZ := date_trunc('month', now());
    next_month_start TIMESTAMPTZ := date_trunc('month', now()) + INTERVAL '1 month';
    month_after_start TIMESTAMPTZ := date_trunc('month', now()) + INTERVAL '2 month';
    current_partition TEXT := 'realm_lifecycle_run_y' || to_char(current_month_start, 'YYYYMM');
    next_partition TEXT := 'realm_lifecycle_run_y' || to_char(next_month_start, 'YYYYMM');
BEGIN
    EXECUTE format(
        'CREATE TABLE IF NOT EXISTS %I PARTITION OF realm_lifecycle_run FOR VALUES FROM (%L) TO (%L)',
        current_partition, current_month_start, next_month_start
    );
    EXECUTE format(
        'CREATE TABLE IF NOT EXISTS %I PARTITION OF realm_lifecycle_run FOR VALUES FROM (%L) TO (%L)',
        next_partition, next_month_start, month_after_start
    );
END $$;
```

**评估**：
- ✅ 完整 RANGE 分区 + DO 块自动建初始分区
- ⚠️ 缺生产环境分区滚动 cron job（**PH-2 评审**）
- ⚠️ 缺 DETACH/DROP 过期分区脚本（**PH-2 评审**）

---

## 3. 应实施但未分区（识别为不合理设计）

### 3.1 `audit_log` (admin_db) — 应按月分区（P0-02）

**现状**：未分区，亿级行/年增长。

**问题**：
- 3 年保留期意味着 ~1.8 亿行（全在单表）
- 索引膨胀，查询性能劣化
- 备份/恢复成本随数据量线性增长

**建议**：
- 按 `created_at` 月度范围分区
- 3 年保留期（36 个分区滚动）
- 应用 ARC-014 "未证明前不引入复杂性" → 但 audit_log 已被 RGS-BAS-007 §4 明确要求分区
- 优先级 **P0**（影响合规 + 性能）

详见 [17-不合理设计 P0-02](17-不合理设计识别与优化建议.md)。

### 3.2 `outbox` × 6 域 — 应按周/月分区（P0-03）

**现状**：未分区，6 域每域千万级/日增长。

**问题**：
- 总量 = 6 × 千万 × 365 ≈ 数十亿/年
- `idx_outbox_pending` / `idx_outbox_in_flight` 索引膨胀
- `status='sent'` 历史数据无法快速清理

**建议**：
- 按 `created_at` 周/月范围分区（依各域吞吐量）
- 已发布（`status='sent'`）的记录短期归档后清理
- 优先级 **P0**（影响 6 域 worker 性能）

详见 [17-不合理设计 P0-03](17-不合理设计识别与优化建议.md)。

### 3.3 `inbox` (economy_db) — 应按时间分区（P2-01）

**现状**：未分区，幂等记录短保留期。

**问题**：
- 处理过的 command 永久保留——按设计应该是 7 天重试窗口后清理
- 数据量会无限增长

**建议**：
- 按 `processed_at` 月度分区
- 30 天保留期
- 优先级 **P2**（影响小，可后置）

### 3.4 `reservations` (economy_db) — 应按时间分区（P2-02）

**现状**：未分区，saga 资金预留。

**问题**：
- 7 天 expired 清理但 DB 层无强保证
- 数据量随 saga 启动频率线性增长

**建议**：
- 按 `created_at` 月度分区
- 7 天保留期
- 优先级 **P2**

### 3.5 `moves` (match_db) — 应按时间分区（P1-07）

**现状**：未分区，卡牌对战操作日志。

**问题**：
- 亿级/年增长
- 老对局的 moves 几乎不会被查询（除 GM 调查）

**建议**：
- 按 `occurred_at` 月度分区
- 1 年保留期（per 业务）
- 优先级 **P1**

详见 [17-不合理设计 P1-07](17-不合理设计识别与优化建议.md)。

### 3.6 `replays` (replay_db) — 应按时间分区（P1-15）

**现状**：未分区，元数据表。

**建议**：
- 按 `expires_at` 月度分区（与 TTL 清理同步）
- 优先级 **P1**

详见 [17-不合理设计 P1-15](17-不合理设计识别与优化建议.md)。

---

## 4. 保留期总览

| 表 | 保留期 | 清理方式 | 引用依据 |
|---|---|---|---|
| `audit_log` | 3 年 | 分区滚动（应实施，未实施）| RGS-REQ-001 §NFR-SE-010 + RGS-BAS-007 §4 |
| `outbox` × 6 域 | 短期（已发布即清）| 分区滚动（应实施，未实施）| RGS-BAS-007 §4 |
| `inbox` | 30 天 | 短期清理（应用层）| RGS-DTL-100 §6 |
| `reservations` | 7 天（expired 即清）| 应用层定时 job | RGS-DTL-100 §3.2 |
| `replays` | RANKED 90 天 / CASUAL 7 天 / ROOM 30 天 | 按 mode 计算 expires_at，cron 清理 | RGS-DTL-038 §3 |
| `matchmaking_tickets` | 5 分钟默认（应用层可覆盖）| 应用层定时 job | RGS-DTL-038 §5.2 |
| `resume_tokens` (SQLite) | 应用层控制（短 TTL）| 应用层定时 job | RGS-SPEC-DTL-041 §6 |
| `transaction_ledger` | 永久 | 不清理 | RGS-DTL-100 §6 |
| `sagas` | 永久 | 不清理 | RGS-DTL-100 §3 |
| 业务主表（players / accounts / inventory / matches / guilds / cards / card_instances） | 永久 | 仅玩家/卡片删除时清理 | 业务需求 |

---

## 5. 分区滚动 SOP（生产环境 cron job）

> PH-2 评审后实施。

```sql
-- 月度分区滚动（每月 1 号 UTC 0:00 跑）
-- 1. 创建下下月分区（确保写入有目标）
DO $$
DECLARE
    target_month_start TIMESTAMPTZ := date_trunc('month', now()) + INTERVAL '2 month';
    month_after_start TIMESTAMPTZ := date_trunc('month', now()) + INTERVAL '3 month';
    target_partition TEXT := 'realm_lifecycle_run_y' || to_char(target_month_start, 'YYYYMM');
BEGIN
    EXECUTE format(
        'CREATE TABLE IF NOT EXISTS %I PARTITION OF realm_lifecycle_run FOR VALUES FROM (%L) TO (%L)',
        target_partition, target_month_start, month_after_start
    );
END $$;

-- 2. DETACH 过期分区（保留 3 年 = 36 个月前的）
DO $$
DECLARE
    expired_month_start TIMESTAMPTZ := date_trunc('month', now()) - INTERVAL '37 month';
    expired_partition TEXT := 'realm_lifecycle_run_y' || to_char(expired_month_start, 'YYYYMM');
BEGIN
    -- 先归档到冷存储（对象存储）再 DROP
    -- 实际实现：pg_dump 该分区 → 上传 S3 → DROP
    EXECUTE format('ALTER TABLE realm_lifecycle_run DETACH PARTITION %I', expired_partition);
    EXECUTE format('DROP TABLE %I', expired_partition);
END $$;
```

---

## 6. SQLite 不支持分区

`resume_tokens` (SQLite) **不支持** PG 声明式分区——SQLite 仅支持表级清理。应用层定时 job 跑 `DELETE FROM resume_tokens WHERE expires_at < now()` 即可（已用 `idx_resume_tokens_expires_at` 索引加速）。

---

## 7. Work 表 cleanup SOP（per RGS-DB-BAS-001 v0.2 §9.3 解决）

> **定位**：解决 [RGS-DB-BAS-001 v0.1 §9.3](../00-基準与治理/RGS-DB-BAS-001_数据库表设计三分类横展开基本设计书_v0.2.md) 缺口——`auctions` / `private_trades` 等"中期"Work 表归类合理但缺 cleanup SOP。

### 7.1 Work 表生命周期总览

| # | 物理表名 | 库 | TTL 字段 | cleanup 触发 | 频率建议 | 现状 |
|---|---|---|---|---|---|---|
| 1 | `player_sessions` | player_db | `expires_at` | `expires_at < now() - 30 days` | 每 1 小时 | 应用层有 `cleanup.last_run_at` 监控（per BAS-007 §6.2 强制全采样）|
| 2 | `reservations` | economy_db | `expires_at` | saga 完成后 / 失败后 / 7 天 TTL | 每 10 分钟 | saga 完成回调里删；TTL 由 job 兜底 |
| 3 | `inbox` | economy_db | `processed_at` | `processed_at < now() - 7 days` | 每 1 小时 | 短期幂等记录，业务允许最大重试窗口 7 天 |
| 4 | `matchmaking_tickets` | match_db | `expires_at` | 匹配完成 / 超时（默认 5 分钟）| 每 30 秒 | 应用层定时 job |
| 5 | `session_subscriptions` | match_db | `expires_at` | 订阅断开 / 超时 | 每 1 分钟 | 应用层定时 job |
| 6 | **`auctions`** | economy_db | `ends_at` + `status` | `ends_at < now() AND status IN (1, 2)` | **每 1 分钟** | **本节新增 SOP（per 9.3）** |
| 7 | **`private_trades`** | economy_db | `closed_at` | `closed_at IS NOT NULL AND closed_at < now() - 7 days` | **每 10 分钟** | **本节新增 SOP（per 9.3）** |
| 8 | `replays` (replay_db) | replay_db | `expires_at` | 按 mode 计算（RANKED 90 / CASUAL 7 / ROOM 30 天）| 每 1 小时 | 应用层 cron |
| 9 | `resume_tokens` (SQLite) | downloads.sqlite | `expires_at` | `expires_at < now()` | 每 10 分钟 | 应用层 DELETE（per §6）|

### 7.2 `auctions` / `private_trades` cleanup SOP（per 9.3 解决核心）

**业务背景**：
- `auctions`（公开拍卖）：`status` 4 状态机 = `1=active / 2=sold / 3=cancelled / 4=expired`，`ends_at` 必填
- `private_trades`（私下交易）：`status` 4 状态机 = `1=proposed / 2=accepted / 3=completed / 4=cancelled`，`closed_at` 成交/撤单/过期时填

**两类 Work 表的共同点**：
- 业务结束后（成交 / 撤单 / 过期）记录仍有合规追溯价值
- 但**不进入 Master 表永久保留**——保留 7-30 天即可
- cleanup SOP 缺失会导致 `status IN (2, 3, 4)` 的"已完成"记录无限堆积

**cleanup job 模板**（per `crates/economy-service/src/jobs/cleanup.rs`，PH-2 实施）：

```sql
-- auctions cleanup（每 1 分钟，partial index 加速）
-- 1) 自动把超期 active 拍卖标记 expired
UPDATE auctions
   SET status = 4,  -- expired
       closed_at = now()
 WHERE status = 1
   AND ends_at < now()
   AND closed_at IS NULL;
-- 注：partial index `idx_auctions_ends_at WHERE status = 1` 加速此 UPDATE

-- 2) 清理已结束 7 天以上的拍卖（保留 7 天合规追溯）
DELETE FROM auctions
 WHERE status IN (2, 3, 4)  -- sold / cancelled / expired
   AND closed_at < now() - INTERVAL '7 days'
   AND closed_at IS NOT NULL;
-- 注：partial index `idx_auctions_status` 不直接覆盖此 DELETE，
--     但 `closed_at` 上无 index，建议 PH-2 评审加 partial index
--     `CREATE INDEX idx_auctions_closed_at ON auctions(closed_at) WHERE status IN (2,3,4)`
```

```sql
-- private_trades cleanup（每 10 分钟）
-- 私下交易没有 ends_at, 依赖 status 状态机 + closed_at
-- 1) 自动超时 proposed 状态（per 业务规则，默认 24 小时）
UPDATE private_trades
   SET status = 4,  -- cancelled
       closed_at = now()
 WHERE status = 1  -- proposed
   AND created_at < now() - INTERVAL '24 hours'
   AND closed_at IS NULL;

-- 2) 清理已结束 7 天以上的私下交易
DELETE FROM private_trades
 WHERE status IN (2, 3, 4)  -- accepted / completed / cancelled
   AND closed_at IS NOT NULL
   AND closed_at < now() - INTERVAL '7 days';
```

### 7.3 cleanup job 监控点（per RGS-BAS-007 §6.2 强制全采样）

| 字段 | 触发 | 频率 | 采样 |
|---|---|---|---|
| `db.work_table.cleanup.started` | cleanup job 启动 | 每 1 小时 | release 必出 |
| `db.work_table.cleanup.completed` | cleanup job 完成（含删除行数）| 每 1 小时 | release 必出 |
| `db.work_table.cleanup.failed` | cleanup job 异常退出 | 偶发 | release 必出 |
| `db.work_table.cleanup.debug.rows_affected_dump` | 详细受影响行 dump | 1/日 | **debug-only** |

### 7.4 已知缺口

- `auctions` 表 `closed_at` 列无 partial index（per §7.2 注释），建议 PH-2 评审加
- `private_trades` `created_at` 列无 partial index 加速超时检测，PH-2 评审
- cleanup job 本身在 `crates/economy-service/src/jobs/cleanup.rs` 待实装（PH-2）

---

## 修订追溯

| 引用 | 路径 |
|---|---|
| BAS-007 | `docs/03-数据经济与交易/RGS-BAS-007_*.md` §4（分区标准）|
| REQ-001 | `docs/00-准备阶段/RGS-REQ-001_*.md` §NFR-SE-010（保留期 3 年）|
| DTL-100 | `docs/01-核心架构与设计模式/RGS-DTL-100_Saga业务模式定义_v0.1.md` §3.2 / §5.3 / §6 |
| DTL-038 | `docs/01-核心架构与设计模式/RGS-DTL-038_*.md` §3 / §5.2 |
| SPEC-DTL-041 | `docs/13-实施规范/RGS-SPEC-DTL-041_*.md` §6 |

> 任何实际 schema 与本文档不一致之处，以 `crates/*/migrations/*.sql` 实际 SQL 为准。
