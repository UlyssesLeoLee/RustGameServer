# 实施前 QA 表（Implementation Pre-flight Q&A）

**RustGameServer — 编码实施前必须解决的 30+ 关键问题**

| 项目 | 内容 |
|---|---|
| 文档编号 | RGS-QA-001 |
| 版本 | 0.2 |
| 制定日 | 2026-08-19 |
| 制定者 | 架构师 |
| 保密级别 | 内部限定（Internal Use Only） |
| 适用许可 | Apache-2.0（本仓库） |
| 配套文档 | RGS-TS-001（技术选型）、RGS-ADR-0008（中间件导入判定）、RGS-ADR-0025（OLU 预算）、RGS-REQ-031（ARC-051）、RGS-REV-001（横断审查） |

---

## 修订历史

| 版本 | 修订日 | 修订者 | 修订内容 |
|---|---|---|---|
| 0.1 | 2026-08-19 | 架构师 | 初版。覆盖协议/性能/部署/测试/团队/文档/工具链 7 大类 30 个问题；每条 Q 带"假设/验证/阻塞/负责人/截止"五字段；优先级 🔴/🟠/🟡/🟢 |
| 0.2 | 2026-08-19 | 架构师 | 通过 `ask_user` 收到 4 项用户决策（DEC-001~004）：①PFAU all-reachable ②节点检测委托 K8s ③ClusterOpsService Active-Active ④First slice 5 域全开。**Blocker 池从 5 减至 2**（Q-003 + Q-025）。新增 §0 决策摘要；Q-001/002/011/022 状态置 ✅；Q-025 截止从 PH-7 提前到 PH-1；Q-015 紧迫性提升为"立即"；§9.4 第一周冲刺重排（DEC-004 5 域全开后）；新增 Q-036（5 域 DTL 同步起草）。下游需同步：RGS-REQ-031 §5.2 / RGS-BAS-031 §2 / RGS-TS-001 §6.2 OLU / 新增 RGS-ADR-0052 |

## 文档目的

> 本文档是**编码实施前的最后一道关卡**——所有"🟠 重要"及以上的问题**必须**在第一行代码提交前解决或明确"接受风险，**带条件进入实施**"。  
> 不是"穷尽一切问题"——而是把"如果不知道答案就开始写代码，**几周后必返工**"的高风险问题摊开，由对应决策人在动手前回答。

## 阅读方式

每条 Q&A 包含 5 个固定字段：

| 字段 | 含义 |
|---|---|
| **假设** | 我（架构师）的当前判断（可能错——需被质疑） |
| **验证** | 怎么确认这个假设/怎么拿到最终答案 |
| **阻塞** | 这条不解决会卡住什么（CR / Sprint / Release） |
| **负责人** | 谁需要回答/拍板 |
| **截止** | 何时必须解决（与 PH 节点对齐） |

## 优先级图例

| 符号 | 含义 | 处理原则 |
|---|---|---|
| 🔴 | **Blocker** | 实施前**必须**有最终答案；不接受"边写边想" |
| 🟠 | **重要** | 第一周内解决；可带条件进入实施（"假设 X 成立，若 PH-1 末不成立则回滚"） |
| 🟡 | **应当** | 第一月内解决；可以并行推进 |
| 🟢 | **Nice** | 实施过程中遇到再解决；不影响主路径 |

## 状态图例

| 符号 | 含义 |
|---|---|
| ☐ Open | 待回答 |
| ✅ Answered | 已回答（答案附在"假设"列，并标注引用来源） |
| 🚧 Accepted Risk | 接受风险继续实施（含明确回滚条件） |
| ⛔ Blocked | 阻塞中（依赖其他 Q 先解决） |

---

## §0 用户决策摘要（v0.2 新增）

> **本节汇总已通过 `ask_user` 拍板的决策，供后续实施、ADR、DTL 同步引用。**

| 决策 ID | Q 编号 | 用户选择 | 落定日 | 关联文档需更新 |
|---|---|---|---|---|
| **DEC-001** | Q-001 | PFAU 全集群确认 = **all-reachable（最严格）** | 2026-08-19 | RGS-REQ-031 §5.2 / DTL-031 §4.2 |
| **DEC-002** | Q-002 | 节点异常检测 = **委托 K8s + 健康检查**（无自定义三态） | 2026-08-19 | DTL-031 §4.2 / FR-PFAU-022 实现 |
| **DEC-003** | Q-011 | ClusterOpsService = **Active-Active 双副本（multi-leader）** | 2026-08-19 | DTL-031 §2 / §10 / TS-001 §6.2 OLU |
| **DEC-004** | Q-022 | First slice = **5 域全开 + 完整 ARC-018/021/042**（8-12 周） | 2026-08-19 | RGS-PLAN-001 新建 / 5 域 DTL / DTL-031 提前 |

### 决策组合影响（关键风险点）

> ⚠️ **DEC-001 + DEC-003 + DEC-004 三决策叠加，系统容错性达最严、一致性最高、范围最广。组合影响：**

1. **OLU 预算**（TS-001 §6.2 估算 19 人·天/周 ≈ 4 SRE 等效）：5 域全开 + multi-leader + K8s probe 调优 = +3.0 人·天/周 → 实际可能 22 人·天/周，**突破 2 SRE 上限**。需重新做 OLU 核算
2. **DTL-031 截止提前**：从 PH-7 提前到 PH-1（first slice 启动前必须就绪），且**必须把 4 个 DEC 一并纳入设计**——这是 ~3 周的高强度 DTL 工作
3. **5 域 DTL 同步启动**：player / economy / match / social / admin 5 份 DTL 必须同时起草，进度必须可视（建议 weekly check-in）
4. **PFAU 完成时延（性能影响）**：all-reachable + 22 节点 + 5 批灰度 + 120s 默认超时 = 单 Feature 升级 p99 = 22 × 5 × 120s + 观察期 300s × 5 ≈ 13 分钟——需要 Q-008 重新校准超时阈值
5. **RPO/RTO 影响**：Active-Active + 5 域 = 单 Pod 失联不影响 COC 写操作，但 all-reachable 让 PFAU 进入 paused——RPO 0 但 RTO 取决于人工介入速度（违反 NFR-COC-004 期望）。建议 PFAU paused 状态自动告警 + Slack 通知

### 下游 ADR 必要性

| 关联 ADR | 必要性 | 建议 |
|---|---|---|
| RGS-ADR-0052 | **建议新增** | "Active-Active ClusterOpsService + all-reachable PFAU 的容错哲学"——单独 ADR 记录这两个决策的代价/回滚条件 |
| RGS-ADR-0025 | 需追加 | OLU 预算条目新增 "first slice 8-12 周 OL 预算" + multi-leader 实例项 |
| RGS-REQ-001 §10 ARC-051 | 需修订 | 把 "原子 = all-reachable" 与 "PFAU 进入 paused 后的 RTO 期望" 写入主需求 |

---

## 目录

- §1 协议与一致性（5 个 Q）
- §2 性能与容量（5 个 Q）
- §3 部署与运维（5 个 Q）
- §4 测试与验证（5 个 Q）
- §5 团队与流程（4 个 Q）
- §6 文档与治理（4 个 Q）
- §7 工具链与生态（4 个 Q）
- §8 风险与例外（3 个 Q）
- §9 总览表与"必须先解决"清单

---

# §1 协议与一致性

## Q-001　PFAU "全集群确认"的 quorum 模型是什么？

| 字段 | 内容 |
|---|---|
| **状态** | ✅ Answered（用户决策 2026-08-19） |
| **优先级** | ~~🔴 Blocker~~ → 已决；下游需同步更新 RGS-REQ-031 §5.2 / DTL-031 |
| **背景** | RGS-REQ-031 §5.2 状态机定义 8 个状态，但"全集群确认"语义未严格定义：strict majority？quorum？all？跨 AZ 部署下 1 个 AZ 失联算不算？ |
| **假设** | ✅ **用户决策**：**全部可达（最严格）**。任何节点失联都阻塞 PFAU 完成到 `canary_confirmed`；超时后进入 `paused` 等人工 retry/skip/rollback（FR-PFAU-010 不变） |
| **代价** | 22 节点规模下，**任一节点异常就会卡住全部 Feature 升级**——但一致性最高，符合 ARC-005 single-writer 严格语义。SLO 影响：单 Feature 升级 p99 完成时间 = max(节点确认时延) × 5 批灰度 + 观察期，与既有 ARC-019 维护模式传播语义一致 |
| **验证** | DTL-031 §4.2 状态机明确"all-reachable 才能进 canary_confirmed"；FR-PFAU-021 默认 120 秒超时是否够，需在 multi-AZ 实测后调（Q-008 解决） |
| **阻塞** | ~~DTL-031 §ClusterOpsService PFAU 状态机实现~~ → 解除；改为 DTL-031 起草前提 |
| **负责人** | 架构师 + SRE Lead |
| **截止** | PH-7 前（DTL-031 同步） |

## Q-002　节点失联 vs 网络分区 vs 健康检查失败的区分机制？

| 字段 | 内容 |
|---|---|
| **状态** | ✅ Answered（用户决策 2026-08-19） |
| **优先级** | ~~🔴 Blocker~~ → 已决；下游需在 DTL-031 §4.2 明确 K8s 集成 |
| **背景** | 现有 3 种"节点异常"被混为一谈：①K8s Pod 异常退出（重启即可）②节点失联（网络/硬件）③网络分区（节点健康但与控制面不通）——三者对 PFAU 的处理完全不同 |
| **假设** | ✅ **用户决策**：**委托给 K8s + 健康检查**。不复用 admin_db 节点心跳（去掉 last_heartbeat_at 自定义逻辑），完全靠 K8s ReadinessProbe / LivenessProbe / Pod lifecycle events。ClusterOpsService 订阅 K8s watch API 感知 Pod phase 变化 |
| **代价** | 自定义代码量最少；但失去"应用层精确感知"——K8s 健康检查通常 5-10 秒延迟，**网络分区**这种"应用层能感知但 K8s 无法立即判断"的情况只能等 K8s probe timeout。**已知风险**：K8s 误判可能让 PFAU 错误地继续；需在 DTL-031 加 K8s probe timeout 调优（建议 10s） |
| **验证** | DTL-031 §4.2 状态机不再有"三态显式区分"逻辑；Chaos Mesh 注入 K8s pod-kill + network-partition 两种场景验证 K8s 通知延迟 |
| **阻塞** | ~~FR-PFAU-022（自动回滚）落地；任何 fail-over 演练~~ → 解除 |
| **负责人** | 架构师 + SRE Lead |
| **截止** | PH-7 前 |

## Q-003　跨 5 DB 的事务一致性如何保证？

| 字段 | 内容 |
|---|---|
| **状态** | ☐ Open |
| **优先级** | 🔴 Blocker |
| **背景** | ARC-008 已定 5 独立 DB；但购买=EC 减货币+PL 加道具、跨边界补偿等业务事务常跨 DB。Saga 已在 ARC-015 落地（单 App 内），但跨 DB 时 SAGA 的"补偿"对象在哪？ |
| **假设** | 跨 DB 业务用 2 个单 DB 事务 + SAGA 编排 + Outbox 触发下游；补偿通过反向操作 + audit 表记录（ARC-022 业务逻辑不入库的反例记录方式） |
| **验证** | 列表出至少 3 个真实跨 DB 业务场景（购买 / 转账 / 跨域奖励），逐一走完 SAGA 流程；写出"补偿延迟上限 p99"的可测指标 |
| **阻塞** | 业务功能实现（任何涉及多 DB 的功能） |
| **负责人** | 架构师 + 经济系统 Lead |
| **截止** | PH-3 前（最早上线经济功能时） |

## Q-004　ARC-018/021/042/051 四个"原子"如何 compose？

| 字段 | 内容 |
|---|---|
| **状态** | ☐ Open |
| **优先级** | 🟠 重要 |
| **背景** | "原子化"在文档里出现 4 次但语义不同：ARC-018（每 App 挂载）/ ARC-021（plugin 切换 tick 边界）/ ARC-042（集群级部署）/ ARC-051（每 Feature 升级）。一个跨 plugin 的 Feature 升级，依赖的 plugin 切换顺序怎么定？ |
| **假设** | 单元由小到大为：plugin tick 边界 < App 挂载 < Feature 升级 < 集群部署；后级可触发前级（Feature 升级 → 触发所属 App 内的 plugin 切换），但不允许反向 |
| **验证** | 画 4 个 ARC 的状态机合并图；用例：升级 Feature X 跨 3 个 plugin，写出完整迁移路径 |
| **阻塞** | 任何跨 plugin / 跨 App 的 Feature 升级 |
| **负责人** | 架构师 |
| **截止** | PH-5 前（首次 Feature 升级演练前） |

## Q-005　NATS JetStream 故障时业务路径如何降级？

| 字段 | 内容 |
|---|---|
| **状态** | ☐ Open |
| **优先级** | 🟠 重要 |
| **背景** | NATS 是事件总线的事实中心（CEM ARC-051 / RGS-TS-001 §3.6），但若 NATS 集群全停：①已发但未消费的事件丢失 ②新事件无消费者 ③ Outbox 堆积 ④ retry 死循环 |
| **假设** | 三段降级：①NATS 全停 → Producer 退回 DB-side Outbox 暂存，Consumer 端暂停消费（业务路径用强一致回退到同步 RPC）②NATS 部分节点停 → 自动重试 + 临时单节点 + 告警 ③NATS 健康但积压 → 限流 Producer + 扩容 Consumer |
| **验证** | Chaos Mesh 注入 NATS pod-kill 演练；降级到同步 RPC 的延迟影响实测；Outbox 暂存的容量上限 |
| **阻塞** | ARC-040 容量规划的 NATS 容量子项；RGS-REQ-001 NFR-AV-* 验收 |
| **负责人** | 架构师 + SRE |
| **截止** | PH-4 前（首次 NATS 上线前） |

---

# §2 性能与容量

## Q-006　100k CCU 准入门禁的分阶段跑通计划何时制定？

| 字段 | 内容 |
|---|---|
| **状态** | ☐ Open |
| **优先级** | 🟠 重要 |
| **背景** | AC-019 要求"100k CCU 全部 NFR 满足"——但没人写过 1k→10k→50k→100k 的逐步爬坡计划。NFR-PE-001~006 目标值（tick p99 < 25ms 等）都是设计值，无实测 |
| **假设** | 4 阶段：①PH-2 本地 1k ②PH-4 预发 10k ③PH-5 staging 50k ④PH-8 预生产 100k；每阶段要 100% NFR 通过才能进下一阶段 |
| **验证** | 起草 RGS-OPS-001 附录 X：load test 阶段计划；列出每阶段的硬件预算和 Pass/Fail 指标 |
| **阻塞** | AC-019 验收 |
| **负责人** | SRE Lead + 架构师 |
| **截止** | PH-2 前（1k 阶段必须先跑通） |

## Q-007　100k CCU 网络带宽预算是多少？

| 字段 | 内容 |
|---|---|
| **状态** | ☐ Open |
| **优先级** | 🟠 重要 |
| **背景** | 100k 玩家 × 状态更新频率 = 总带宽。NFR-PE-* 仅给了延迟目标，没给带宽预算。若按"每玩家 1Hz 状态包 × 200B" = 20MB/s = 160Mbps——单 AZ 可承受，但跨 AZ 复制后是 3 倍 |
| **假设** | 100k CCU × 平均 2Hz 状态同步 × 200B 压缩后 = 40MB/s = 320Mbps 单向；预留 50% 余量 = 480Mbps；建议 ingress ≥ 1Gbps，AZ 间 ≥ 2Gbps |
| **验证** | k6 脚本实测 + OTel 网络埋点；RGS-TS-001 §3.11.1 主机规格需重审 |
| **阻塞** | RGS-OPS-001 §1.1 硬件最低要求表；多 AZ 部署规划 |
| **负责人** | SRE Lead |
| **截止** | PH-3 前 |

## Q-008　PFAU 120 秒确认超时在 multi-AZ 部署下是否足够？

| 字段 | 内容 |
|---|---|
| **状态** | ☐ Open |
| **优先级** | 🟡 应当 |
| **背景** | FR-PFAU-021 默认 120 秒。multi-AZ 部署下，跨 AZ 节点确认可能有 50-200ms 延迟 × 22 节点 + NATS 序列化 + 跨 AZ 网络——批量确认时间可能超 120 秒 |
| **假设** | 120 秒仅够单 AZ 内 22 节点；多 AZ 部署需重新核算（建议 180-300 秒） |
| **验证** | 跨 AZ 部署实测；或 RGS-DTL-031 §4.2 给出"超时 = max(120s, 1.5 × 已知 P99 跨 AZ 确认时长)"的动态公式 |
| **阻塞** | 多 AZ 部署上线 |
| **负责人** | SRE Lead |
| **截止** | PH-5 前（多 AZ 上线前） |

## Q-009　sqlx 编译期校验的 CI 缓存策略？

| 字段 | 内容 |
|---|---|
| **状态** | ☐ Open |
| **优先级** | 🟡 应当 |
| **背景** | sqlx 0.7+ `query!()` 宏在编译期需要 DB schema（在 `sqlx prepare` 缓存的 `.sqlx/` 目录）。CI 缓存失效 + 漂移检测未自动化 |
| **假设** | CI 必须：①缓存 `.sqlx/` 目录 ②每 PR 跑 `cargo sqlx prepare --check`（schema 漂移阻断）③每 PG migration 触发 `sqlx prepare` 重生成 |
| **验证** | 跑通一次完整 PR：故意改 migration 不更新 `.sqlx/`，看是否阻断；恢复后再跑 |
| **阻塞** | 所有 App CI 流水线 |
| **负责人** | 架构师 + SRE |
| **截止** | PH-1 前 |

## Q-010　Rhai 沙箱在 20Hz tick 路径上的性能边界？

| 字段 | 内容 |
|---|---|
| **状态** | ☐ Open |
| **优先级** | 🟠 重要 |
| **背景** | Rhai 解释执行 + 同进程 = 单次 plugin call 性能未知。ARC-021 把插件定位为"活动规则"，但**没有强制排除"plugin 进 tick 路径"**——若运营误用，单个 plugin 可拖垮 RT 进程 |
| **假设** | plugin call 预算 ≤ 1ms（RT 20Hz tick 50ms 中留 2% 给 plugin）；超预算的 plugin 自动 disabled 并告警 |
| **验证** | 写一个 1ms 超时检测的 Rhai 拦截器；做 P99 性能基准；若超阈值需在 RT 路径上拒绝 plugin 加载 |
| **阻塞** | ARC-021 落地（任何 plugin 不能在 tick 路径上跑） |
| **负责人** | 架构师 |
| **截止** | PH-3 前（plugin 首次上线前） |

---

# §3 部署与运维

## Q-011　ClusterOpsService 的高可用设计？

| 字段 | 内容 |
|---|---|
| **状态** | ✅ Answered（用户决策 2026-08-19） |
| **优先级** | ~~🔴 Blocker~~ → 已决；下游 DTL-031 §2 需重写为 multi-leader 拓扑 |
| **背景** | ClusterOpsService 失联意味着无法：升级 Feature / 注册新 Feature / 重放事件 / 丢弃 DLQ。RGS-REQ-031 NFR-COC-004 降级到只读模式，但**写入路径**怎么办？ |
| **假设** | ✅ **用户决策**：**Active-Active 双副本（multi-leader）**。两个独立 Pod 同时接收 COC 写请求，通过事件溯源 + admin_db 行级乐观锁（基于 `request_id` 幂等，FR-API-003）协调；每个 leader 负责不同分区（按 feature_id 哈希）以减少冲突。失联**不会**让 COC 写操作停摆——这是 4 个选项中可用性最高的 |
| **代价** | ①运维最复杂（双 leader 数据同步、冲突解决）②OLU 至少 +1.0 人·天/周（多 1 个 leader 实例的监控/告警/升级）③对 admin_db 写压力 +1 倍 ④必须实现严格幂等（FR-API-003 request_id 是关键），否则双写不一致 |
| **验证** | DTL-031 §2 组件图改为 multi-leader 拓扑；DTL-031 §10 高可用设计：①leader 失联时另一 leader 接管全分区（短暂双写窗口）②故障注入验证 0 RPO ③admin_db 行锁验证幂等；OLU 预算核算（同步更新 TS-001 §6.2） |
| **阻塞** | ~~ARC-051 落地（任何 COC 写操作）~~ → 解除；DTL-031 §2 需重写 |
| **负责人** | 架构师 + SRE Lead |
| **截止** | PH-7 前 |
| **关联** | 与 Q-001 all-reachable 决策共同影响：PFAU 完成时需两个 leader 都确认"all-reachable"，这意味着失联容忍度为 0（任一 Pod 失联即暂停）——**multi-leader 与 all-reachable 叠加后系统可用性需重新核算** |

## Q-012　跨 5 DB 的 migration 工具？

| 字段 | 内容 |
|---|---|
| **状态** | ☐ Open |
| **优先级** | 🟠 重要 |
| **背景** | sqlx-migrate 仅单 DB；5 库独立 migration 无法保证"全成功或全回滚" |
| **假设** | 自研轻量工具 `migrator` 编排多 DB migration：①按 dependency 顺序执行 ②任意一个失败全停 ③记录每 DB 状态到 `admin_db.migration_runs` 表 ④可断点续跑 |
| **验证** | 模拟 5 DB 中第 3 个失败，看前 2 个是否回滚（事务隔离）or 标记待续跑（视场景） |
| **阻塞** | 任何跨 DB 的 schema 变更；AC-COC-002 端到端演练 |
| **负责人** | 架构师 + DBA |
| **截止** | PH-2 前 |

## Q-013　客户端 3 引擎的 ABI 兼容矩阵？

| 字段 | 内容 |
|---|---|
| **状态** | ☐ Open |
|---|---|
| **优先级** | 🟠 重要 |
| **背景** | RGS-ADR-0023 选 C ABI，但实际打包 3 套独立体系：Unity（C# DllImport / IL2CPP）、Unreal（C++ UFunction）、Godot（GDExtension）。每套有独立 ABI 兼容矩阵（iOS sim / arm64 / WebGL / MinGW / 32-bit） |
| **假设** | 目标矩阵：iOS arm64 + Android arm64 + Windows x64 + macOS x64/arm64 + WebGL (Unity) + Linux x64；32-bit 与 MinGW 不支持 |
| **验证** | 起草 RGS-DTL-008 §X「目标平台兼容矩阵」表；与客户端团队对齐 |
| **阻塞** | 客户端首次发版；AC-SDK-001 三引擎一致性 |
| **负责人** | 客户端 Lead |
| **截止** | PH-1 前 |

## Q-014　PFAU 自动回滚失败时升级到集群级回滚的路径？

| 字段 | 内容 |
|---|---|
| **状态** | ☐ Open |
| **优先级** | 🟡 应当 |
| **背景** | FR-PFAU-022 自动回滚是"回退至上一稳定版本"，但若回滚本身失败（如新版本的 schema 不兼容回滚），无降级路径 |
| **假设** | 三段降级：①PFAU 自动回滚（健康）→ ②ClusterOpsService 触发 ARC-042 集群级 helm rollback（与 PFAU 状态机解耦）→ ③人工介入（紧急止血：网关维护模式 + 切流量） |
| **验证** | DTL-031 §4.2 状态机明确 PFAU 失败 → 集群级回滚的状态转换条件 |
| **阻塞** | 重大事故恢复演练 |
| **负责人** | 架构师 + SRE Lead |
| **截止** | PH-5 前 |

## Q-015　OLU 预算已逼近上限，新组件如何 OLU 核算？

| 字段 | 内容 |
|---|---|
| **状态** | ☐ Open |
| **优先级** | 🟠 重要 |
| **背景** | RGS-TS-001 §6.2 估算 19 人·天/周 ≈ 4 SRE 等效，NFR-OP-010 上限 2 SRE（≤20 人·天/周）。当前架构已吃 95% 预算——22+ App 全部上线后必然超 |
| **假设** | 任何新组件（含本文档提出的 migrator、自研 PFAU controller）必须经 ARC-014 + ARC-026 双门禁 OLU 核算；OLU 公式：运维周小时 = 部署(0.5) + 监控(0.3) + 故障处理预估(0.5×MTBF/MTTR) + 升级(0.2×每年次数) + 文档(0.1) |
| **验证** | ISS-092 (COC UI) 和 ISS-065 (13 域未算) 必须先补齐；新组件审批模板里加 OLU 字段 |
| **阻塞** | NFR-OP-010 验收；任何新组件引入 |
| **负责人** | 架构师 + SRE Lead |
| **截止** | 持续（每个新组件前必须算） |

---

# §4 测试与验证

## Q-016　24 份 TST 是设计书不是测试代码——先建 testkit 还是边写边抽？

| 字段 | 内容 |
|---|---|
| **状态** | ☐ Open |
| **优先级** | 🟠 重要 |
| **背景** | 24 × 3 = 72 份 TST 是测试**设计**，每条用例 → Rust 代码 + setup + fixture 通常 5-10x 膨胀。仅 ARC-031 的 233 个用例就要 1500-2000 行。共享 mock / fixture 库 `testkit` 是否先建？ |
| **假设** | 先建 `crates/testkit/`，提供：①PG/Redis/NATS/K8s 的 testcontainers 封装 ②通用 mock helpers ③test fixture builders；第一周投入 |
| **验证** | 写完 testkit 后用 ARC-031 的 78 条 UT 跑一遍，统计开发效率提升 |
| **阻塞** | 所有测试代码工作 |
| **负责人** | 架构师 + QA Lead |
| **截止** | PH-1 前（PH-1 末必须有第一批 UT 可跑） |

## Q-017　chaos engineering 实施范围？

| 字段 | 内容 |
|---|---|
| **状态** | ☐ Open |
| **优先级** | 🟠 重要 |
| **背景** | AC-COC-007（AdminService 故障注入）是**唯一**显式 chaos 用例。但生产中必然发生：K8s 节点失联、PVC 满、磁盘 IO 抖动、NATS 脑裂、PG 主备切换、Redis Cluster 脑裂、网络分区 |
| **假设** | 用 Chaos Mesh 覆盖 12 个核心场景（K8s pod-kill / 网络延迟 / 网络分区 / 磁盘压力 / 进程 OOM / DB 主备切换 / 缓存击穿 / 事件积压 / ...），每场景对应 1 个 GameDay 演练 |
| **验证** | RGS-OPS-001 §X 列出 12 个 chaos 场景 + 演练频率 + 通过标准 |
| **阻塞** | NFR-AV-* 验收；GA 标准 |
| **负责人** | SRE Lead |
| **截止** | PH-6 前（首次 GameDay） |

## Q-018　100k CCU 负载测试的硬件预算？

| 字段 | 内容 |
|---|---|
| **状态** | ☐ Open |
| **优先级** | 🟡 应当 |
| **背景** | 100k CCU 负载需要多少台负载机（k6 runner）+ 多少台被测节点？预算未列 |
| **假设** | 1 个 k6 runner ≈ 模拟 10k CCU（HTTP/2 多路复用）；10 台 runner = 100k CCU；被测集群 T2 档（生产规格），不需要更高级别 |
| **验证** | 在 staging 跑 50k 阶段时实测；更新 RGS-TS-001 §6.2 硬件预算表 |
| **阻塞** | PH-5 50k 阶段 |
| **负责人** | SRE Lead |
| **截止** | PH-4 前 |

## Q-019　Mock 框架选型？

| 字段 | 内容 |
|---|---|
| **状态** | ☐ Open |
| **优先级** | 🟡 应当 |
| **背景** | Rust mock 框架选择影响测试代码风格。`mockall`（最流行，宏生成）vs `wiremock`（HTTP 专用）vs `mockito`（轻量）vs 自研 trait |
| **假设** | `mockall` 作主（trait-based 自动 mock）；`wiremock-rs` 专用于 COC UI 外部 API 集成测试；其他场景用 testcontainers |
| **验证** | ARC-031 UT-31 的 78 条用例中 5-10 条用 `mockall` 跑通，对比代码量和可读性 |
| **阻塞** | Q-016 testkit 落地 |
| **负责人** | QA Lead |
| **截止** | PH-1 前 |

## Q-020　性能回归测试（criterion）嵌入 CI 的策略？

| 字段 | 内容 |
|---|---|
| **状态** | ☐ Open |
| **优先级** | 🟡 应当 |
| **背景** | criterion 基准运行时间长（分钟级），CI 中全量跑不现实；但若不跑，性能回归会无声发生 |
| **假设** | 策略：①PR 仅跑标记为 `@bench-quick` 的子集（< 60s）②Nightly 跑全量 ③基准结果与 main 分支对比，差异 > 5% 标红 ④历史数据存入专门的 bench-baseline repo |
| **验证** | 跑通一次 PR + nightly；写 RGS-OPS-001 附录「性能回归策略」 |
| **阻塞** | NFR-PE-* 持续监控 |
| **负责人** | SRE Lead + QA Lead |
| **截止** | PH-2 前 |

---

# §5 团队与流程

## Q-021　24+ 份 untracked 文档的提交策略？

| 字段 | 内容 |
|---|---|
| **状态** | ☐ Open |
| **优先级** | 🟠 重要 |
| **背景** | 24+ 份先前会话遗留 untracked 文档（TST-00~07、addendum TST、9 份 addendum REQ、13 个 scripts/*.py、RGS-OPS-001）——这些是"幽灵资产"：未来 review 时无法追溯决策、无法 diff、无法 blame |
| **假设** | 分 3 批提交：① RGS-OPS-001 单独 commit（与 ARC-051 无关，先入库）②TST-00~07 主题域 7 批（每主题 1 commit）③addendum TST + scripts 1 批；每 commit 写清"补登 v0.1" |
| **验证** | git log --diff-filter=A --name-only 显示新增文件；所有 untracked 消失 |
| **阻塞** | 所有 git-blame 追溯；任何团队 review |
| **负责人** | 架构师 |
| **截止** | PH-1 前（最优先） |

## Q-022　"最窄可跑通路径"的范围裁剪提案？

| 字段 | 内容 |
|---|---|
| **状态** | ✅ Answered（用户决策 2026-08-19） |
| **优先级** | ~~🟠 重要~~ → 已决；DTL-031 + 5 域 DTL 需同步起草 |
| **背景** | 整个 22+ 域不可能同步实现。需要定义"first slice"——能端到端跑通的最窄功能集 |
| **假设** | ✅ **用户决策**：**5 域全开 + 完整 ARC-018/021/042**。first slice 包含 5 个服务（player / economy / match / social / admin）、5 个 DB 全部 schema、3 个核心 ARC（ARC-018 挂载 + ARC-021 热插拔 + ARC-042 集群部署）联动。8-12 周首次端到端跑通 |
| **代价** | ①**风险最高**（对比 §9.4 原 first-slice-recommended 的 4-6 周）——5 域 DTL 同时起草，任何一个返工都会阻塞整体进度 ②OLU 预算（TS-001 §6.2）需从 +1.0 → +3.0 人·天/周，可能触顶 ③DTL-031 必须在 first slice 启动**前**完成（之前是 PH-7 截止） ④需立刻启动 5 域 DTL 起草（多 5 份 DTL 同时写） |
| **关键前置条件** | ①DTL-031 优先级提升到 PH-1 前 ②5 域 DTL 同步起草（player / economy / match / social / admin）③5 域 OLU 核算（ISS-065 推进）④DTL-031 起草时把 Q-001 all-reachable + Q-011 Active-Active 全部纳入 |
| **验证** | RGS-PLAN-001（实施计划）文档立即起草：列出 5 域范围 + ARC-018/021/042 联动 + 排除项（ARC-019/020/051/039 不在 first slice）+ 8-12 周里程碑 + 每周通过标准 |
| **阻塞** | ~~一切后续实施~~ → 解除；RGS-PLAN-001 起草与 DTL-031 同步启动 |
| **负责人** | 架构师 + 项目负责人 + 5 域 Lead |
| **截止** | PH-1 前起草 RGS-PLAN-001 与 DTL-031；first slice 端到端跑通 8-12 周内 |
| **关联** | Q-001 + Q-011 + Q-022 三个激进决策叠加：all-reachable × Active-Active × 5 域全开 = 系统容错最严、一致性最高、范围最广。**OLU 预算已是 95% 上限**（TS-001 §6.2），三决策叠加后必须强制重新核算 |

## Q-023　3 套 RBAC 体系如何对齐？

| 字段 | 内容 |
|---|---|
| **状态** | ☐ Open |
| **优先级** | 🟡 应当 |
| **背景** | GM 后台 RBAC（RGS-REQ-007） + COC UI RBAC（RGS-REQ-031 §7.3） + 客户端 SDK RBAC（若有）——三套独立定义，跨页面操作时权限边界不清 |
| **假设** | 主体框架沿用 RGS-BAS-001 §7.3 既有 `viewer/operator/admin`；COC UI 在此之上加 `cluster_operator/cluster_admin` 角色（已定义）；客户端 SDK 暂用最小集（无 SRE 角色） |
| **验证** | 列出 1 个运营人员典型操作路径（如"先在 GM 后台封禁玩家 → 再在 COC UI 回滚一个 plugin"），验证角色权限一致 |
| **阻塞** | GM 后台 + COC UI 集成测试 |
| **负责人** | 架构师 + SRE Lead |
| **截止** | PH-4 前 |

## Q-024　治理闭环 CIR（CheckItem Report）的执行节奏？

| 字段 | 内容 |
|---|---|
| **状态** | ☐ Open |
|---|---|
| **优先级** | 🟡 应当 |
| **背景** | 22+ 域 + 5 项 TBD + 13 项 ISS-065 + 新增 ISS-092——治理对象繁多。多久跑一次 CIR 清理？ |
| **假设** | 每月 1 次 CIR，3 人 2 小时；新 ARC/ADR/TBD/ISS 提交时附"check item self-check" |
| **验证** | RGS-BAS-009 §X 写 CIR 节奏 + 模板；与 RGS-REQ-001 §15 FR-GOV-040 对齐 |
| **阻塞** | 治理闭环持续运行 |
| **负责人** | 架构师 |
| **截止** | PH-2 前（首次 CIR） |

---

# §6 文档与治理

## Q-025　DTL-031 详细设计的优先级与时间窗口？

| 字段 | 内容 |
|---|---|
| **状态** | ☐ Open |
| **优先级** | 🔴 Blocker |
| **背景** | RGS-DTL-031（ClusterOpsService 详细设计）尚未写。Q-001/Q-002/Q-011 都依赖它。但 DTL 写完需要 Q-001~Q-005 的答案——循环依赖 |
| **假设** | 先做 RGS-DTL-031 的"接口契约"和"组件图"小节（不依赖 Q-001~Q-005 的答案），剩余"状态机迁移"和"高可用"小节等 Q-001~Q-005 解决后再补 |
| **验证** | DTL-031 起草分两批：先骨架后血肉 |
| **阻塞** | ClusterOpsService 代码实现；ARC-051 落地 |
| **负责人** | 架构师 |
| **截止** | PH-7 前（与 ARC-051 上线同步） |

## Q-026　RGS-TS-001 中 5 项 TBD 的 PH 节点评审排期？

| 字段 | 内容 |
|---|---|
| **状态** | ☐ Open |
| **优先级** | 🟠 重要 |
| **背景** | TS-001 §5.2 列了 5 项 TBD（TBD-077 Connect / TBD-MNT-002 CNPG / TBD-NEURO-002 vLLM / TBD-LOG-001 Loki / TBD-SEC-003 Vault）+ 2 项版本升级。但 PH 节点评审排期未定 |
| **假设** | 排期：TBD-MNT-002 在 PH-2 前（DB 上线前）；TBD-LOG-001 在 PH-3 前（首次日志后端前）；TBD-SEC-003 在 PH-4 前（密钥轮换演练前）；TBD-NEURO-002 在 PH-5 前（智能层接入前）；TBD-077 出现外部需求时；TBD-VERSION-001/002 每年 1 月与 7 月 |
| **验证** | RGS-BAS-009 §X 写排期表；CI 校验 TBD 是否到时未解决 |
| **阻塞** | 各项组件上线 |
| **负责人** | 架构师 |
| **截止** | 每个 TBD 自己的 PH 节点前 |

## Q-027　22+ 域 DTL 的"未决项"如何管理？

| 字段 | 内容 |
|---|---|
| **状态** | ☐ Open |
| **优先级** | 🟡 应当 |
| **背景** | 22+ 域 DTL 中有大量"待 PH-X 前确定"项（如 TBD-DTL-005 §7 Rhai 已决但留有"白名单函数持续维护"等）。集中管理 vs 每域独立管理？ |
| **假设** | 集中管理：附件 D §1.3 是唯一权威 TBD 表；每域 DTL 引用 `TBD-{域}-nnn` 但不维护本域 TBD 表 |
| **验证** | 抽查 3 份 DTL，确认 TBD 引用一致；附件 D 总数与 DTL 引用数对账 |
| **阻塞** | TBD 治理一致性 |
| **负责人** | 架构师 |
| **截止** | PH-1 前 |

## Q-028　文档版本与代码版本同步策略？

| 字段 | 内容 |
|---|---|
| **状态** | ☐ Open |
| **优先级** | 🟡 应当 |
| **背景** | 当前文档版本手动管理（"修订历史"表）；代码改完文档可能没改。无 CI 校验 |
| **假设** | 策略：①每 PR 必须更新对应文档版本号（CI 校验关联 .md 文件）②每月 1 次"文档漂移检查"（grep 代码中引用的 FR/NFR ID 是否都在文档中） |
| **验证** | 故意改代码不更新文档，看 CI 是否阻断 |
| **阻塞** | 治理一致性 |
| **负责人** | 架构师 + SRE |
| **截止** | PH-2 前 |

---

# §7 工具链与生态

## Q-029　MinIO AGPL-3.0 的潜在传染路径与应对？

| 字段 | 内容 |
|---|---|
| **状态** | ☐ Open |
| **优先级** | 🟡 应当 |
| **背景** | 2024-01 MinIO 切 AGPL-3.0。当前只客户端调用没问题，**但若未来要 fork / 二次开发**（如加 CDN 边缘缓存集成），AGPL 传染性会强制整套服务端也走 AGPL——可能与 Apache-2.0 仓库 license 冲突 |
| **假设** | 三段应对：①只客户端调用 OK（现状）②小修小补（patch backport）→ 改用社区版或商业版 ③深度二次开发 → 评估 license 整体迁移至 AGPL-3.0（需负责人决议） |
| **验证** | 法务 review；写 RGS-LIC-001 license 风险表 |
| **阻塞** | 任何 MinIO 深度定制需求 |
| **负责人** | 法务 + 架构师 |
| **截止** | PH-3 前（首次 MinIO 部署前） |

## Q-030　sqlx 0.7+ 升级到 0.8+ 的影响？

| 字段 | 内容 |
|---|---|
| **状态** | ☐ Open |
| **优先级** | 🟢 Nice |
| **背景** | sqlx 0.8 可能引入 breaking change；升级影响所有 5 DB 连接的 App |
| **假设** | 升级窗口随 sqlx 上游发布；评估 breaking change 后单独 ADR 决策 |
| **验证** | 跟踪 sqlx 仓库 release notes |
| **阻塞** | 无（不影响当前） |
| **负责人** | 架构师 |
| **截止** | sqlx 0.8 正式发布时 |

## Q-031　tonic 0.10+ 与 hyper 1.x 版本耦合的同步升级窗口？

| 字段 | 内容 |
|---|---|
| **状态** | ☐ Open |
| **优先级** | 🟢 Nice |
| **背景** | tonic 0.10+ 依赖 hyper 1.x；任一升级可能影响另一 |
| **假设** | 升级前看 tonic release notes；若需同步升则一起升 |
| **验证** | cargo tree 检查版本耦合 |
| **阻塞** | 无（不影响当前） |
| **负责人** | 架构师 |
| **截止** | 上游主版本发布时 |

## Q-032　Rust 主版本升级（1.83 → 1.84）的窗口？

| 字段 | 内容 |
|---|---|
| **状态** | ☐ Open |
| **优先级** | 🟢 Nice |
| **背景** | Rust 双月发布；MSRV 升级需评估所有 crate 兼容性 |
| **假设** | 每年 1 月与 7 月各升一次；升级前跑完整测试套件 |
| **验证** | RGS-TS-001 §4.3 TBD-VERSION-001 排期 |
| **阻塞** | 无（不影响当前） |
| **负责人** | 架构师 |
| **截止** | 2027-01 与 2027-07 |

---

# §8 风险与例外

## Q-033　ISS-092 (COC UI OLU) 和 ISS-065 (13 域未算) 的处理顺序？

| 字段 | 内容 |
|---|---|
| **状态** | ☐ Open |
| **优先级** | 🟠 重要 |
| **背景** | 附件 D §5 记录 13 域未做 OLU 核算，加上 ISS-092 = 14 项待办。无明确处理顺序 |
| **假设** | 处理顺序：①ISS-092（COC UI，与 ARC-051 同步） → ②按 PH 阶段倒推（PH-1 上线的 5 个域先算）→ ③每 PH 阶段开始前清零一次 |
| **验证** | RGS-BAS-009 §5 写 OLU 排期表；CI 校验每 PR 不新增未算项 |
| **阻塞** | AC-GOV-005（OLU 台账余额非负） |
| **负责人** | 架构师 + SRE Lead |
| **截止** | PH-2 前 ISS-092；PH 节点前其余 ISS |

## Q-034　项目"复活"标准——什么时候算 MVP？什么时候算 GA？

| 字段 | 内容 |
|---|---|
| **状态** | ☐ Open |
| **优先级** | 🟠 重要 |
| **背景** | 当前无明确定义：什么状态下可以对外开放玩家（GA）？什么状态算可演示（MVP）？ |
| **假设** | MVP：1k CCU 跑通 5 个核心域（player/economy/match/social/admin），PH-4 末判定；GA：100k CCU 通过 AC-019 全部 NFR，PH-8 末判定；中间状态按"功能完成度 + 稳定性"两个维度评估 |
| **验证** | 起草 RGS-REQ-027 §11 升级 MVP/GA 判定标准；与项目负责人确认 |
| **阻塞** | 商业化 / 玩家接入 |
| **负责人** | 项目负责人 + 架构师 |
| **截止** | PH-3 前（MVP 标准） |

## Q-035　"代码 + 文档 + 测试" 三位一体的 CI 校验门禁是否一次搭好？

| 字段 | 内容 |
|---|---|
| **状态** | ☐ Open |
| **优先级** | 🟡 应当 |
| **背景** | 现有 `scripts/*.py` 工具是 IPA 标准化阶段产物，但跨工具的"文档 ↔ 代码 ↔ 测试"一致性无统一 CI 门禁 |
| **假设** | 写 `.github/workflows/ci.yml`：① build + test（已有）② lint + format（已有）③ audit + deny（已有）④ 文档交叉引用校验（`scripts/check-docs-consistency.sh` 增强）⑤ FR/NFR ID 在代码中引用校验（grep） |
| **验证** | 跑通一次完整 PR；故意改代码不更新文档看是否阻断 |
| **阻塞** | 治理闭环持续运行 |
| **负责人** | 架构师 + SRE |
| **截止** | PH-2 前 |

---

# §9 总览表与"必须先解决"清单

## 9.1 总览（v0.2 更新：4 项已答）

| 类别 | 总数 | 🔴 待答 | ✅ 已答 | 🟠 重要 | 🟡 应当 | 🟢 Nice |
|---|---|---|---|---|---|---|
| §1 协议与一致性 | 5 | 1 | 2 | 2 | 0 | 0 |
| §2 性能与容量 | 5 | 0 | 0 | 3 | 2 | 0 |
| §3 部署与运维 | 5 | 0 | 1 | 3 | 1 | 0 |
| §4 测试与验证 | 5 | 0 | 0 | 2 | 3 | 0 |
| §5 团队与流程 | 4 | 0 | 1 | 2 | 1 | 0 |
| §6 文档与治理 | 4 | 1 | 0 | 1 | 2 | 0 |
| §7 工具链与生态 | 4 | 0 | 0 | 0 | 1 | 3 |
| §8 风险与例外 | 3 | 0 | 0 | 2 | 1 | 0 |
| **合计** | **35** | **2** | **4** | **15** | **11** | **3** |

## 9.2 🔴 Blocker 清单（**已从 5 减至 2**）

| Q | 主题 | 负责人 | 截止 | 备注 |
|---|---|---|---|---|
| Q-003 | 跨 5 DB 的事务一致性方案 | 架构师 + 经济系统 Lead | PH-3 前 | 仍 Blocker；SAGA 跨 DB 补偿对象待定 |
| Q-025 | DTL-031 详细设计的优先级与时间窗口 | 架构师 | **PH-1 前**（从 PH-7 提前） | DEC-004 决策后 DTL-031 必须 first slice 启动前就绪 |

> ~~Q-001 / Q-002 / Q-011 已于 2026-08-19 通过用户决策解决（见 §0 决策摘要）~~

## 9.3 🟠 重要清单（**DEC-004 决策后调整**）

| Q | 主题 | 优先级 | 截止 | 备注 |
|---|---|---|---|---|
| Q-004 | ARC-018/021/042/051 四个"原子"如何 compose | 🟠 | PH-5 前 | DEC-004 加重紧迫性 |
| Q-005 | NATS JetStream 故障时业务路径如何降级 | 🟠 | PH-4 前 | 5 域全开后事件流压力 ↑ |
| Q-006 | 100k CCU 准入门禁的分阶段跑通计划 | 🟠 | PH-2 前 | DEC-004 范围扩大 |
| Q-007 | 100k CCU 网络带宽预算 | 🟠 | PH-3 前 | 5 域全开需重算 |
| Q-010 | Rhai 沙箱在 20Hz tick 路径上的性能边界 | 🟠 | PH-3 前 | 不变 |
| Q-012 | 跨 5 DB 的 migration 工具 | 🟠 | **PH-1 前**（从 PH-2 提前） | DEC-004 5 域全开需要 |
| Q-013 | 客户端 3 引擎的 ABI 兼容矩阵 | 🟠 | PH-1 前 | 不变 |
| Q-015 | OLU 预算核算机制 | 🟠 | **立即**（DEC-001/003/004 叠加后 OLU 必然触顶） | 紧迫性 ↑ |
| Q-016 | 24 份 TST → 共享 testkit 库 | 🟠 | PH-1 前 | 不变 |
| Q-017 | chaos engineering 实施范围 | 🟠 | PH-6 前 | DEC-003 Active-Active 需新 chaos 场景 |
| Q-021 | 24+ 份 untracked 文档的提交策略 | 🟠 | PH-1 前（最优先） | 不变 |
| ~~Q-022~~ | ~~"最窄可跑通路径"范围裁剪~~ | ~~🟠~~ | ~~PH-1 前~~ | ✅ 已答（DEC-004 5 域全开 8-12 周） |
| Q-026 | RGS-TS-001 中 5 项 TBD 排期 | 🟠 | 各 TBD 自身 PH 节点 | 不变 |
| Q-033 | ISS-092 + ISS-065 处理顺序 | 🟠 | **PH-1 前**（从 PH-2 提前） | DEC-004 加重紧迫性 |
| Q-034 | MVP/GA 判定标准 | 🟠 | PH-3 前 | DEC-004 5 域全开需重定义 MVP |
| **🆕 Q-036** | 5 域 DTL 同步起草计划（DEC-004 触发） | 🟠 | PH-1 前 | player / economy / match / social / admin 5 份 DTL 并行起草 |

## 9.4 第一周冲刺计划（**DEC-004 调整后**，5-7 人·天）

按"先做最便宜高 ROI 的事"原则，**DEC-004 决策后工作项重排**：

| 顺序 | 工作 | 投入 | 收益 |
|---|---|---|---|
| 1 | **Q-021** 整理 24+ 份 untracked 文档入仓 | 2h | 治理闭环基础；解锁 git blame |
| 2 | **RGS-PLAN-001** 起草（Q-022 已答 → 直接写计划，不再是"提案"） | 4h | 5 域 + 3 ARC 联动的时间线 |
| 3 | **DTL-031 骨架**（纳入 DEC-001/003/004 决策） | 1.5d | 2 个 Blocker（Q-003 + Q-025）之一解决；DTL-031 截止提前到 PH-1 |
| 4 | **Q-015 OLU 预算重新核算**（DEC-003 + DEC-004 叠加） | 0.5d | 立即判断是否需要申请额外 SRE 编制 |
| 5 | **5 域 DTL 同步启动**（Q-036 触发） | 排入 8-12 周冲刺 | 5 份 DTL 并行起草 |
| 6 | **Q-016** 起草 `crates/testkit/` 骨架 | 2d | 5 域并行开发前置依赖 |

合计：第一周 ~7-9 人·天，Q-022 + Q-001/002/011 全部从 Blocker 池移出，**仅剩 2 个真 Blocker**（Q-003 跨 DB 事务 + Q-025 DTL-031），且 Q-025 已在第一周冲刺中推进。

---

> **本文档不是"等待完美答案才能动笔"的阻断物**——它的目的是**把已知盲点显式化**。  
> 凡标 🔴 的 Q 必须在第一行代码提交前有结论；🟠 可带条件推进；🟡/🟢 在主路径推进中并行解决。  
> 任何 Q 答案变化**必须**更新本文档相应行（含版本号 bump），并触发对应 ADR / DTL / REQ 的 review。
