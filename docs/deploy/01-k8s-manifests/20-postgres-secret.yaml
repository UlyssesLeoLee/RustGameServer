# 20-postgres-secret.yaml
# 角色：PostgreSQL 18.6 超级用户密码 + 5 域 DB 用户凭证（per ARC-008 5 独立 DB 原则）
# 状态：🔴 NO-GO 占位（per RGS-EXEC-001 v0.3）
# 实际值写入条件：SRE 具名 + 加密仓管理（**严禁明文提交到 git**）
# 实际部署：
#   kubectl apply -f 20-postgres-secret.yaml -n PLACEHOLDER_NAMESPACE
# 实际值由 sealed-secrets / external-secrets 注入（DBA + SRE 联合生成）
# 关联：
#   - RGS-IMPL-001 §3.4 Secret 管理
#   - RGS-TS-001 v0.6 §5.2 PostgreSQL 18.4 / 18.6 选型
#   - ARC-008 5 独立 DB 原则
#   - ADR-0052 cluster_ops_db 独立（Active-Active 跨节点调谐）
---
# 超级用户（postgres）密码 — 实际值：REPLACE_BEFORE_DEPLOY（k3s apply 前由 sealed-secrets 替换）
apiVersion: v1
kind: Secret
metadata:
  name: PLACEHOLDER_POSTGRES_SUPERUSER_SECRET  # 实际值：postgres-superuser
  namespace: PLACEHOLDER_NAMESPACE
  labels:
    app.kubernetes.io/name: postgres
    app.kubernetes.io/component: database
    app.kubernetes.io/part-of: rust-game-server
    rust-game-server.io/tier: data
type: Opaque
stringData:
  POSTGRES_PASSWORD: "REPLACE_BEFORE_DEPLOY_SUPERUSER_PASSWORD"
  POSTGRES_USER: "postgres"
---
# 5 域 DB 用户凭证（player / economy / match / social / admin）+ cluster_ops
# 实际值：每个 DB 一个用户 + 各自密码（REPLACE_BEFORE_DEPLOY_*）
apiVersion: v1
kind: Secret
metadata:
  name: PLACEHOLDER_PLAYER_DB_SECRET  # 实际值：player-db-credentials
  namespace: PLACEHOLDER_NAMESPACE
  labels:
    app.kubernetes.io/name: player
    app.kubernetes.io/component: database
    rust-game-server.io/domain: player
type: Opaque
stringData:
  username: "player_user"
  password: "REPLACE_BEFORE_DEPLOY_PLAYER_PASSWORD"
  database: "player_db"
  url: "postgres://player_user:REPLACE_BEFORE_DEPLOY_PLAYER_PASSWORD@PLACEHOLDER_POSTGRES_SVC_NAME:5432/player_db"
---
apiVersion: v1
kind: Secret
metadata:
  name: PLACEHOLDER_ECONOMY_DB_SECRET
  namespace: PLACEHOLDER_NAMESPACE
  labels:
    app.kubernetes.io/name: economy
    app.kubernetes.io/component: database
    rust-game-server.io/domain: economy
    rust-game-server.io/saga-critical: "true"
type: Opaque
stringData:
  username: "economy_user"
  password: "REPLACE_BEFORE_DEPLOY_ECONOMY_PASSWORD"
  database: "economy_db"
  url: "postgres://economy_user:REPLACE_BEFORE_DEPLOY_ECONOMY_PASSWORD@PLACEHOLDER_POSTGRES_SVC_NAME:5432/economy_db"
---
apiVersion: v1
kind: Secret
metadata:
  name: PLACEHOLDER_MATCH_DB_SECRET
  namespace: PLACEHOLDER_NAMESPACE
  labels:
    app.kubernetes.io/name: match
    app.kubernetes.io/component: database
    rust-game-server.io/domain: match
type: Opaque
stringData:
  username: "match_user"
  password: "REPLACE_BEFORE_DEPLOY_MATCH_PASSWORD"
  database: "match_db"
  url: "postgres://match_user:REPLACE_BEFORE_DEPLOY_MATCH_PASSWORD@PLACEHOLDER_POSTGRES_SVC_NAME:5432/match_db"
---
apiVersion: v1
kind: Secret
metadata:
  name: PLACEHOLDER_SOCIAL_DB_SECRET
  namespace: PLACEHOLDER_NAMESPACE
  labels:
    app.kubernetes.io/name: social
    app.kubernetes.io/component: database
    rust-game-server.io/domain: social
type: Opaque
stringData:
  username: "social_user"
  password: "REPLACE_BEFORE_DEPLOY_SOCIAL_PASSWORD"
  database: "social_db"
  url: "postgres://social_user:REPLACE_BEFORE_DEPLOY_SOCIAL_PASSWORD@PLACEHOLDER_POSTGRES_SVC_NAME:5432/social_db"
---
apiVersion: v1
kind: Secret
metadata:
  name: PLACEHOLDER_ADMIN_DB_SECRET
  namespace: PLACEHOLDER_NAMESPACE
  labels:
    app.kubernetes.io/name: admin
    app.kubernetes.io/component: database
    rust-game-server.io/domain: admin
    rust-game-server.io/coc: "true"
type: Opaque
stringData:
  username: "admin_user"
  password: "REPLACE_BEFORE_DEPLOY_ADMIN_PASSWORD"
  database: "admin_db"
  url: "postgres://admin_user:REPLACE_BEFORE_DEPLOY_ADMIN_PASSWORD@PLACEHOLDER_POSTGRES_SVC_NAME:5432/admin_db"
---
apiVersion: v1
kind: Secret
metadata:
  name: PLACEHOLDER_CLUSTER_OPS_DB_SECRET
  namespace: PLACEHOLDER_NAMESPACE
  labels:
    app.kubernetes.io/name: cluster-ops
    app.kubernetes.io/component: database
    rust-game-server.io/domain: cluster-ops
    rust-game-server.io/pfau-critical: "true"
type: Opaque
stringData:
  username: "cluster_ops_user"
  password: "REPLACE_BEFORE_DEPLOY_CLUSTER_OPS_PASSWORD"
  database: "cluster_ops_db"
  url: "postgres://cluster_ops_user:REPLACE_BEFORE_DEPLOY_CLUSTER_OPS_PASSWORD@PLACEHOLDER_POSTGRES_SVC_NAME:5432/cluster_ops_db"
