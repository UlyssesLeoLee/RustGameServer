# DEPRECATED — Empty directory, custom HTML PoC, wrong direction

**2026-09-09 15:10 JST Mavis 标记**: 此目录是 custom HTML PoC (game.html + game-case1-5.png), 错误方向. 真实前端是 zsyz_client (Cocos2d 编译后 binary), 不是自造 HTML.

**正确路径**: rgs-shim-rust v0.3.1 (proto-test.js 10/10 passed, 字节级 zsyz_server 协议兼容). 见 `tools/rgs-shim-rust/docs/ERLANG_TO_RGS_MIGRATION.md`.

**清理**: 原始文件已删除 (game.html + README + 5 截图 + 3 测试), 目录保留空. 待 root 用户用 `Remove-Item` 删目录.
