# Grafana datasources provisioning（per WBS v0.3 §2A.5 WF-1-53.12）
apiVersion: 1
datasources:
  - name: Prometheus
    type: prometheus
    access: proxy
    url: http://prometheus:9090
    isDefault: true
    editable: true